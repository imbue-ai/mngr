# AGENTS.md

Codex agents must read and follow `./CLAUDE.md` for this repository.

This file exists as a compatibility shim so Codex discovers the repository
instructions without requiring a custom `project_doc_fallback_filenames`
configuration.
