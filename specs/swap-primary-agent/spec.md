# Swap primary agent

WIP spec — being written via `/architect`. See the active Q&A in the originating Claude Code session.

Goal: separate the services/bootstrap "primary" agent in minds from the initial chat agent the user interacts with, so destroying or restarting one no longer affects the other.
