Added the recovery-verdict-policy blueprint plan (`blueprint/recovery-verdict-policy/plan-recovery-verdict-policy.md`), the spec for this branch: the unconditional start-only entry dispatch (the recovery page fires the idempotent `mngr start` restart on page entry with no host-state knowledge, which makes the classifier tiers display-only), dropping the surgical restart tier, the raw-host-state recovery classifier, the `UNREACHABLE` provider state, provider `CRASHED` corrections, error-level restart-failure reporting, and the stop/window-close flow.
