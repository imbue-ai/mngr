- minds-launch-to-msg.yml: build job moves from `ubuntu-latest` to the self-hosted `minds-runner` Mac. Required to bundle Mac-native uv/git/lima into the resulting .app (the Linux runner shipped ELF binaries that crashed the desktop client at `uv` exec). Build and verify now serialize on the same runner.
- repo: `.gitignore` now also ignores `**/scripts/*.local.sh` (one-off local test harnesses), `apps/minds_workspace_server/package-lock.json`, and `**/.DS_Store`.
- specs: update `specs/electron-desktop-app/` (spec + concise) to reflect the shipped minds desktop-app architecture.
- minds-launch-to-msg.yml: swap headline screenshot source -- per-window Playwright `.win.png` captures are now embedded in the GitHub step summary, with full-desktop `screencapture -x` shots demoted to `.desktop.png` forensic dupes in the artifact. CDP page activation does not move macOS WindowServer z-order, so the full-desktop captures routinely showed the unauthenticated /welcome BrowserWindow instead of the actual chat / projects / approval pages the e2e script was driving. The per-page captures bypass WindowServer (DOM-to-raster via CDP) and consistently show the correct content.
- minds-launch-to-msg.yml: stop publishing screenshots to the `ci-screenshots` orphan git branch -- that branch grew to ~1.2GB of PNGs and was downloaded by every clone of the repo. Screenshots now ride only in the per-run `launch-to-first-message-<run_id>` GitHub Actions artifact (auto-expires per `retention-days`). The job summary now lists a manifest of milestone -> filename instead of inline images; viewers download the artifact zip to inspect. The orphan branch is being deleted from origin in the same change.
- minds-launch-to-msg.yml: tee the launch-to-msg + slack flow Python script's stdout+stderr to `/tmp/launch-to-msg-logs/e2e-stdout.log` and bundle that directory into the diagnostics artifact. The e2e script's structured loguru output (phase progressions, kick attempts, navigation events) was previously only visible in GHA's console log (expires); the artifact zip is the durable post-mortem surface.
- launch_to_msg_e2e.py: skip the periodic kick when no chat window is currently visible (replaces `find_chat_window(ctx) or win` with a `find_chat_window` check + early-skip). During the latchkey approval flow `win` is often the `/requests/<id>` page, which has no textarea; the previous behavior logged a spurious warning every KICK_INTERVAL.
- minds-playwright-vanilla.yml: upload Playwright report on every run (was failure-only) and tag the artifact with `run_attempt`, so a passing rerun no longer erases a failing attempt's screenshots. Add the `html` reporter so the artifact contains a browsable report with embedded screenshots, traces, and the action timeline.
- launch-smoke.spec.js: take + attach a `main-window-final` screenshot in a `finally` block so the html report shows the launcher's landing state on passing runs too, not just failures. Pairs with the artifact-always upload added earlier.
- minds-playwright-vanilla.yml -> minds-macos-launch.yml; workflow display name -> `minds macOS launch`; job `vanilla-launch` -> `macos-launch`. The spec file (`launch-smoke.spec.js`) carries the test's purpose; the workflow's name now describes what the CI infrastructure is (macOS launch). README and CI-DESIGN.md updated to match.
- launch-smoke spec + fixtures.js: pick the content WebContentsView for the final-state screenshot. `app.firstWindow()` returns the chrome view (URL `/_chrome`), which renders only the title bar -- screenshotting it gave a one-row strip on the HTML report. Added `pickContentWindow` helper in fixtures.js (JS twin of `_pick_content_page` in `e2e_workspace_runner.py`) that polls `app.windows()` for a page on the backend origin without the `/_chrome` prefix. Spec uses it in the `finally` block; falls back to chrome if the content view hasn't appeared yet.
- launch-smoke.spec.js -> macos-launch.spec.js; the directory `apps/minds/test/e2e/` already conveys the minds + e2e context, so the spec name drops the redundant prefix and just describes what's tested (macOS launch). README, CI-DESIGN.md, and the workflow's spec invocation updated to match.
- docs: add `apps/minds/docs/release.md` runbook covering the minds.app release procedure: pinning mngr SHA + FCT tag + ToDesktop build, refreshing FCT pilot vendor/mngr, re-cutting the version tag, triggering build/verify CI, local drive, and shipping via the ToDesktop web UI. Includes the failure-mode notes that bit us on this iteration (dirty `.mngr/settings.toml` aborting in-place checkout, `gh workflow run` duplicate runs from FCT cwd, GHA sidebar entries surviving rename).
- restore the supply-chain cooldowns disabled "temporarily while CI churns": `apps/minds/pnpm-workspace.yaml` `minimumReleaseAge: 20160` with main's `minimumReleaseAgeExclude: [latchkey, "@imbue-ai/detent"]`; `apps/minds/electron/pyproject/pyproject.toml` `[tool.uv] exclude-newer = "14 days"` (the rolling form main has since adopted). The cited modal-1.4.3 blocker is now 17 days old, comfortably outside the 14-day window. ToDesktop CI uses `--frozen-lockfile` so neither gate fires during install; both only bite during fresh resolution.
- revert `libs/mngr_lima/imbue/mngr_lima/limactl.py:155` default `timeout: float = 600.0` (was 1800.0 on the branch). The function's only caller (`libs/mngr_lima/imbue/mngr_lima/instance.py:467-473`) passes `timeout=self.config.vm_start_timeout_seconds` explicitly, so the function default was never used. Restores apps/minds-only branch scope per `f794b3e98`. Drop the matching `libs/mngr_lima/changelog/wz-minds_onboard.md` entry.
- drop the duplicate `port <= 0` guard at the top of `apps/minds/imbue/minds/cli/run.py` and its `test_run_rejects_port_zero` unit test. mngr_forward already raises a near-identical `click.UsageError("--reverse local port must be > 0, ...")` at the boundary that actually consumes the value, and neither Electron's backend.js nor any CI/dev script passes `--port 0`. Defensive ornament that fires for no real caller.
- Linux-compat sweep follow-ups: `first_launch_prefetch.py` now picks the Lima image cache root per-platform (macOS `~/Library/Caches/lima`, Linux XDG `~/.cache/lima`) so the prefetch is reusable on Linux (previously wrote to a path Lima would never read). `apps/minds/docs/desktop-app.md` is no longer claiming a ".AppImage for Linux" target -- the release pipeline ships macOS arm64 only. The backend.js PATH-append comment now acknowledges that `/usr/local/bin` is also conventional on Linux.
- Cleanup pre-merge: drop personal/local-only tooling from the branch index so it doesn't leak into main. `apps/minds/scripts/drive-minds.sh` (overnight harness) + `apps/minds/scripts/revive-agent-chat.sh` (backend-revival escape hatch, zero production callers) renamed to `.local.sh` on disk -- the existing `**/scripts/*.local.sh` gitignore makes them untracked. `apps/minds/.claude/commands/minds-ops.md` (personal slash command) renamed to `.local.md`, picked up by `**/*.local.md`. `.github/workflows/minds-macos-launch.yml` triggers changed from `branches: [wz/minds_onboard]` to `[main]` so the workflow continues running post-merge. Documentation references updated to match.
