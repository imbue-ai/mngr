Added `specs/minds-concept-taxonomy/` — a code-grounded taxonomy and standardization spec for every concept that exists today across `apps/minds`, the forever-claude-template, and `libs/mngr*`.

Contents: a canonical glossary (`README.md`), a plain-language / user-facing definition set with flagged naming decisions (`HUMAN-READABLE.md`), an overloaded-term reconciliation with concrete rename recommendations (`CROSS-CUTTING.md`), a consolidated doc/code divergence register (`DOC-CODE-DIVERGENCES.md`), and the full per-concept evidence under `groups/`.

Docs-only; no code changes.
