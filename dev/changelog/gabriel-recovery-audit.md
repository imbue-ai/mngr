Added `minds_background_process_audit.md` at the repo root: an inventory of every persistent background process, daemon, and long-lived thread the minds desktop app spawns or relies on, organized as three concentric rings (Electron shell, host-side desktop client, agent-container services).

Updated references in this doc from the old `forever-claude-template` name to `default-workspace-template`, matching the repo rename.

Documentation only; no behavior changes.
