- Remove dead "release" branch references from CI. There is no `release` branch -- releases are cut from `main` as `v*` tags, and release-marked tests run via the TMR workflow. The CI workflow (`.github/workflows/ci.yml`) no longer triggers on a `release` branch push, drops the always-true `github.ref != 'refs/heads/release'` guards on the offload/acceptance/docker jobs, and deletes the two jobs (`test-docker-release`, `test-release`) that were gated to `refs/heads/release` push and therefore never ran. Also refresh the stale "Release Tests" description in `style_guide.md` and drop the `release` branch from the changelog-ratchet PR-branch skip in `test_meta_ratchets.py`.
