## Provider uniformity review -- Round 2 + core providers shape doc + release-test proposal

- Adds `specs/provider-shape.md`: a forward-looking, prescriptive "core providers doc" describing what an mngr provider OUGHT to look like -- user contract (including a new §1.8 making the N-agents-per-host invariant explicit), capability-flag honesty, default values that providers should share (security/CIDR, idle, auto-shutdown, resources, regions, sizes, images, tags, keys, exposure), lifecycle override hooks, error classification, operator commands, test requirements, anti-patterns observed today, taxonomy table, 20-box implementer checklist, and open design questions. Heavy MUST/SHOULD/MAY language with `file.py:lineno` cites to current code. §3.1 documents the new uniform cloud-trio default `allowed_ssh_cidrs = ("0.0.0.0/0",)` (SSH is key-only; the firewall is the operator's tightening knob) rather than the earlier round-2 fail-closed recommendation.

- Adds `specs/provider-uniformity-review-lifecycle.md`: deep-dive descriptive review of create/stop/start/destroy/cleanup behavior across all 9 providers with the full lifecycle matrix and `CleanupFailedGroup` adoption matrix. Reflects the new base `stop_host` that takes `stop_reason` as a parameter (commit `dec33516a`).

- Adds `specs/provider-uniformity-review-v2.md`: synthesis layer with cross-round status table (what changed since 2026-06-11), 17 new findings from the equal-depth pass on vultr/ovh/lima/docker/ssh, and a 31-item cross-cutting punch list ordered by impact x ease. Includes a §0 "2026-06-15 status update" tracking ~80 provider-touching commits since the original v2 writeup -- 4 findings FIXED (notably F-SNAP-2 via deletion of dead snapshot client surface), 4 findings WORSENED/SUPERSEDED (notably the fail-closed CIDR prescription, which the codebase rejected in favor of the cloud-trio-uniform `0.0.0.0/0` default), 6 partially fixed, and 7 new findings (live agent discovery now in base, per-field AWS agent tags, GCP per-host image override, etc.).

- Adds `specs/provider-release-tests.md`: proposal for a common release-test suite -- five multi-step "trips" (full lifecycle + sketchy kill + GC; second-agent-on-same-host; idle auto-shutdown contract; snapshot-survives-destroy; error classification) that amortize the per-provider boot cost across many assertions. Each trip step maps explicitly to one or more sections of the shape doc, and every cross-provider divergence is flagged with an [INCONSISTENT] callout linking to the round-1, round-2, or 2026-06-15 audit finding it pins. Trip 1b exercises shape §1.8 (N agents per host) and surfaces the AWS 50-tag cap + Azure/GCP/Vultr/OVH offline-mirror gap.

- 2026-06-15 audit highlights: Modal's per-agent JSON-per-file storage (`/hosts/<host_id>/<agent_id>.json`) is the model; AWS got per-field agent tags (commit `446d6a964`) up to the EC2 50-tag wall; live agent discovery is no longer Modal-only (commit `8cca7406d` lifted in-container scan into the VPS-Docker base); the `auto_shutdown_minutes` -> `auto_shutdown_seconds` rename is complete; `_record_stop_reason` was removed in favor of a base `stop_host(..., stop_reason=...)` parameter; the VpsClient dead snapshot surface was deleted entirely.
