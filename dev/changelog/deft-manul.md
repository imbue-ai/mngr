Bump the `test-docker-electron` CI job's Node.js to 24.15.0, pnpm to 10.33.4, and Python to 3.12.13 to match the new exact-version pins in `apps/minds/package.json` and the repo-root `.python-version`. Tighten `.python-version` from `3.12` to `3.12.13` so `uv`-managed Python is exact across all developer machines.
