- The minds deployment/services orchestrator script was renamed from `apps/minds/scripts/test_deployments.py` to `apps/minds/scripts/run_deployment_tests.py` so its name no longer matches pytest's `test_*.py` collection glob (it is a click CLI, not a pytest module). The `just minds-test-deployment*` / `minds-test-services-against` recipes and `specs/minds-deployment-tests.md` were updated to the new path.
