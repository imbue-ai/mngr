Add a `just list-pool-servers` recipe: lists the bare-metal SERVERS (boxes) for the activated minds env -- what you pick a `--server-id` from when baking a pool slice. There was no `minds pool` subcommand for this, so it forwards to `mngr imbue_cloud admin server list`, resolving the tier's host_pool DSN the same way `minds pool list` does (from Vault for staging/production, from the per-env secrets.toml for dev/ci) and passing it via the `MINDS_HOST_POOL_DSN` env var so the DSN never lands in argv.
