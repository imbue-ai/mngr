Add the `blueprint/workspace-claude-auth-settings-env/` plan: move workspace Claude auth off mngr host env vars into the shared `CLAUDE_CONFIG_DIR/settings.json` env block, make the in-workspace login modal the sole auth surface (subscription via `claude setup-token`, raw API key, and a new "Sign in with Imbue" LiteLLM-key paste flow), remove the AI provider from the workspace create route, upgrade the auth-change restart to cover worker agents with "please continue" messaging, and add a manual migration script for existing workspaces.
