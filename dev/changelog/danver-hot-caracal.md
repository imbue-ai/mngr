- Created two Linear tickets in the CI Timing project (MIND-141, MIND-142) based on empirical analysis of ~5,600 CI runs from 2026-05-22 to 2026-07-22, and dispatched two Pi Kimi agents via `sculpt` to work on them directly.

- **MIND-141**: the `build-minds-snapshot` -> `test-minds-snapshot` chain (merged 2026-06-21, commit 6c7bc4cec8) is now the CI critical path ~70-90% of the time, adding ~10.3-10.9 min to PR CI. The snapshot image is rebuilt from scratch on every run with no cross-run caching.

- **MIND-142**: `test-offload` (unit+integration via offload) degraded ~6.5 min -> ~8.0-8.8 min over the same period. Test count grew +46% (10.8k -> 15.9k). The fan-out phase is ~72% per-batch fixed overhead (117 batches of ~77s, median ~28s actual test time). A full investigation report with per-phase numbers and estimated wins was generated by a separate investigation agent. Speculative retry/hedging behavior is explicitly out of scope per stakeholder constraint.
