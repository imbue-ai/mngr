Add `blueprint/lima-host-start-race/plan-lima-host-start-race.md`, the implementation blueprint for fixing the Lima "status Running but sshd not up yet" race that made a concurrent `mngr start` fail spuriously during a host boot. The plan captures the verified root cause, the boot-in-flight detection approach (checking the process table for an in-flight `limactl start`), the `STARTING` classification surfaced across routing and discovery, the gc catch-widening, and the release-tier behavior-guard test. The earlier standalone analysis doc (`specs/host-start-readiness-race.md`) was absorbed into this plan and removed; the full provider audit and entry-point survey remain in git history.

Propagate the mngr_lima release-test-helper coverage omissions (`_lima_btrfs_release_helper.py` and the new `_lima_host_start_race_helper.py`) into the root `pyproject.toml`'s `[tool.coverage.run].omit`, so the combined-coverage gate matches the subproject's omit set and the `test_top_level_coverage_omit_covers_subproject_omits` meta-ratchet passes.
