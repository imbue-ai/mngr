Add `specs/host-start-readiness-race.md`, an analysis of the lima "status Running but sshd not up yet" race that made a concurrent `mngr start` fail spuriously during a host boot. The doc verifies the root cause, surveys how every entry point behaves against a mid-boot host, enumerates candidate resolutions with trade-offs, and recommends a scoped readiness gate at the `ensure_host_started` seam. Also logs the related `cli/start.py` comment/code disagreement in `specs/uncertainties.md`. No code changes.
