Exempt the rolling `mngr/release-candidate` integration branch from the changelog gate (`scripts/check_changelog_entries.py`), alongside the existing `mngr/changelog-consolidation` exemption. The release-candidate branch is latest `main` plus the review queue rather than a feature, so it has no user-visible change to describe and previously required a synthetic per-project entry on every rebuild.
