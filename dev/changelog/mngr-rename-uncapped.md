Updated root-level references for the `mngr_uncapped_claude` plugin rename to
`mngr_robinhood_claude`: the top-level `README.md` sub-projects list, the
`--cov=imbue.mngr_robinhood_claude` coverage entry in the root `pyproject.toml`,
the `robinhood-claude` entry in `scripts/make_cli_docs.py`'s secondary-command
set, the `specs/robinhood-claude/` spec directory, and `uv.lock`.
