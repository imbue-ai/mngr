Added the first behavioral unit tests for the LiteLLM proxy app (`app_test.py`): one verifying every `LITELLM_CONFIG` model maps its public `model_name` to the matching `anthropic/<model_name>` routing target with the expected `ANTHROPIC_API_KEY` env-var reference, and one verifying `_write_config_file` serializes the config with the `os.environ/<NAME>` secret references and per-model routing LiteLLM needs at startup.
