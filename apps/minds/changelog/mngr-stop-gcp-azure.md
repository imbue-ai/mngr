Gave the desktop-client `test_start_creation_imbue_cloud_ai_with_local_compute_mints_litellm_key` test a `@pytest.mark.timeout(30)` budget: it is a deterministic sync test but its setup (fresh ConcurrencyGroups + a recording http-server fixture) can exceed the default 10s pytest-timeout when offload sandboxes are contended. No behavior change.
