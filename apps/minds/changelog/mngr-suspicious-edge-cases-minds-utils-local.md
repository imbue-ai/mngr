Documented the intentional edge-case behavior in `minds/utils`: clarifying comments now explain why `emit_event` is a deliberate no-op in JSON mode and silently suppresses message-less events in HUMAN mode. Also unified the console logging metadata in `logging.py` into a single `_LEVEL_STYLES` table (loguru level name, color, and prefix per level), replacing the separate `_LEVEL_MAP` dict and the if-chain in `_format_user_message` with data-driven lookups. No behavior change.
