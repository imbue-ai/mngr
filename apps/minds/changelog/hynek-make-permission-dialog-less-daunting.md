Simplified the latchkey predefined-permission approval dialog for non-technical users. By default it now shows a read-only, informative list of the permissions the agent is requesting (no checkboxes), with only Approve and Deny actions. A small "Adjust permissions" link reveals the full per-permission checkbox editor (the previous appearance) for users who want fine-grained control.
