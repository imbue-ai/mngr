No user-facing change. This branch is local, not-for-merge scaffolding for manually testing the fast Lima build workflow (pre-baked image cache, issue #2306) in the minds Electron app against a dev env. It temporarily overrides two `desktop_client/templates.py` constants (the default forever-claude-template repo URL and `FALLBACK_BRANCH`) so the pre-baked-image gate fires for a non-dev-loop default-workspace create while keeping the in-VM agent's vendored mngr matched to the dev binary. These overrides must be reverted before any real PR/release.
