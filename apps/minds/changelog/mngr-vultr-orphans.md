Mark `test_call_reports_nonzero_exit_for_unknown_command` (in `mngr_caller_test.py`) as `@pytest.mark.flaky`. The test starts a forkserver and preloads `imbue.mngr.main` before invoking the CLI; that startup occasionally exceeds the 10s pytest-timeout when offload sandboxes are contended (the same class of flake already marked on the agent_creator litellm-key tests). Offload retries flaky tests automatically.
