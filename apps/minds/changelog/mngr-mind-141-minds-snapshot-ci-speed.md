Speed up the `build-minds-snapshot` -> `test-minds-snapshot` CI chain (the PR critical path) by scoping the `minds_snapshot_resume` suite's offload discovery to just `apps/minds/test_snapshot_resume.py` and `apps/minds/test_sync_e2e.py` (MIND-141). Local `pytest --collect-only` on the CI runner drops from ~90s (full-monorepo collection) to a few seconds; the tests themselves are unchanged. See `dev/changelog/mngr-mind-141-minds-snapshot-ci-speed.md` for details and the new guard tests.
