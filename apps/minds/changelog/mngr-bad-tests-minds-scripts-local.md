- `scripts/build_test.py`: the bundled-limactl entitlement guard now skips (instead of failing with an opaque `AssertionError`) when the `node` toolchain is absent, and surfaces node's stderr when `todesktop.js` fails to evaluate so a broken config is diagnosable.
- Renamed the deployment-test orchestrator `scripts/test_deployments.py` to `scripts/run_deployment_tests.py`: it is a click CLI (run via `just minds-test-deployment*`), not a pytest module, so the old `test_` name caused pytest to needlessly collect it (0 tests) and misled readers grepping for the deployment-test suite.
