Hardened two flaky creation unit tests (`test_start_creation_api_key_ai_does_not_mint_litellm_key` and `test_start_creation_subscription_ai_does_not_mint_litellm_key`) by raising their per-test pytest timeout above the 10s global budget. Their setup spins up fresh ConcurrencyGroups and a recording http-server fixture, which occasionally overran the 10s limit when offload sandboxes were heavily contended; the extra headroom stops the spurious timeouts. No product behavior change.
