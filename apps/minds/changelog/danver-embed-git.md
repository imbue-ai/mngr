The desktop app's bundled git is now fully managed on macOS and Linux: a version-pinned, SHA256-verified dugite-native payload (git 2.53.0) downloaded per `scripts/git-manifest.json`, replacing the previous unpinned copy of the build machine's Xcode CLT git on macOS and the incomplete bare-binary copy on Linux. The payload is self-contained (binary, `libexec/git-core` helpers, templates, system gitconfig, and a Linux CA bundle) and ships with GPLv2 license and provenance notices. Windows keeps its already-pinned MinGit path.

The backend child environment now exports the git environment contract (`GIT_EXEC_PATH`, `GIT_TEMPLATE_DIR`, `GIT_CONFIG_SYSTEM`, plus `GIT_SSL_CAINFO` on Linux) in both dev and packaged modes, because the payload binaries bake in an empty prefix and a bare PATH prepend is insufficient (https clones would fail with "'remote-https' is not a git command"). Dev mode now also runs the bundled git itself, so the binary and its exec-path can never be version-skewed. The backend logs the bundled git version once at startup.

`scripts/ensure-binaries.js` now replaces stale git payloads on dev machines via a `.dugite-tag` marker, and a new acceptance test (`test_bundled_git.py`) proves the payload end-to-end on the current platform, including a hermetic HTTPS clone through `git-remote-https` against a local TLS server. Unit tests in `scripts/build_test.py` guard the manifest schema and its consistency with the downloader.

The payload's symlinks (142 `libexec/git-core` builtins pointing at the multicall `git` binary, plus the `git-remote-https`/`ftp`/`ftps` helpers pointing at `git-remote-http`) are now replaced at download time with tiny POSIX-sh shims that exec their former targets. ToDesktop's app-source zip follows symlinks, so the raw payload zipped to ~608MB (142 materialized copies of the 3.4MB git binary) and pushed the upload past the 600MB `uploadSizeLimit`; the shimmed payload zips to its real ~141MB. `build.js` now also fails fast if the staged `resources/` tree would inflate past a symlink-materialization threshold or exceed the ToDesktop upload budget, with new unit tests covering the shim conversion (including that a payload-escaping symlink aborts it), the archive-size measurement, the budget guard, and a drift guard keeping the conversion ordered before the `.dugite-tag` write. The acceptance test gains a phase that converts its extracted payload with the real production function and re-proves the HTTPS clone and dashed-builtin dispatch through the shims.

Documentation: `docs/desktop-app.md` now describes the managed payload accurately (the previous text about copying git from the build machine was stale) and gains an "Updating the bundled git" runbook. The design lives in `specs/minds-managed-git/concise.md`.
