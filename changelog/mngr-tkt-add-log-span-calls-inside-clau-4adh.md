Add per-step `log_span` instrumentation inside `ClaudeAgent._setup_per_agent_config_dir`. Each logical step (`mkdir_and_version_warn`, `resolve_remote_work_dir`, `build_claude_json`, `build_settings_json`, `read_remote_credentials`, `merge_keychain_api_key`, `sync_user_resources` / `rsync_claude_home_directories`, `provision_local_credentials`, `write_generated_files`) is wrapped in its own span so TRACE-level logging produces an attributable per-step elapsed-time breakdown. No behavior change. Useful when profiling the `mngr create` Claude provisioning cost on remote hosts.
