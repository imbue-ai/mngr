Drop four block comments in `libs/mngr_wait/` that each sat one line above a single function call whose name already said the same thing (e.g. `# Poll the initial state` above `initial_state = poll_target_state(resolved)`). They added no abstraction over the next line's identifier, so they read as noise. Block comments that summarise a multi-line block at a higher level are left in place.
