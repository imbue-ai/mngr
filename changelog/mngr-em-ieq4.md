Switch `imbue.mngr.utils.polling` to `time.monotonic()` for elapsed-time measurement. Previously used `time.time()` (wall clock), which can jump backwards under NTP correction or system suspend/resume, leading to negative elapsed values and erratic polling behavior. `time.monotonic()` is unaffected by wall-clock changes and is the correct primitive for measuring intervals. No public API change.
