Update README performance numbers to match current reality. The "Fast" bullet and timing block claimed `mngr create` finishes in ~1.5s, but that number was measured on 2026-02-24; on current main a Claude agent (the example's default type) takes ~5.5s end-to-end due to per-agent Claude config-dir setup. Updated the bullet and the timing example to distinguish light agent types (under 2s) from Claude agents (~5s), and shortened the example output to the relevant `real` lines.
