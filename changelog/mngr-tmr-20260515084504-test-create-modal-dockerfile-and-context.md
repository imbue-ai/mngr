Tighten the `test_create_modal_dockerfile_and_context` e2e test so it actually exercises `-b file=` and `-b context-dir=`: the test now builds a Dockerfile that COPYs a marker file from the context directory and verifies via `mngr exec` that the resulting image both runs the expected `python:3.12-slim` interpreter and contains the copied marker file. Previously it only asserted that `mngr create` exited 0.
