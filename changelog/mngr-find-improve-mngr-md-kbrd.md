- Fixed two stale CLI references in user-facing docs: the snapshot-and-fork example in `README.md` now uses `--format "{snapshot_id}"` (the field `snapshot create` actually emits) instead of `{id}` (a `snapshot list`-only field that would have raised `KeyError`); and `libs/mngr/docs/customization.md` now describes the current `--transfer` flag for `mngr create` work_dir setup instead of the long-removed `--worktree`/`--copy-source` flags.
