- Fixed: the `mngr forward` subprocess no longer pegs a CPU after agents or hosts come and go. Reverse-tunnel bookkeeping in `SSHTunnelManager` is now pruned when the agent is destroyed (so paramiko transport threads exit instead of being kept alive by repeated re-establishment attempts), and the 30s health-check loop applies per-tunnel exponential backoff and drops a tunnel after 10 consecutive failed repair attempts.
