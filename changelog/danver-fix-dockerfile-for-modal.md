Fix Dockerfile multi-stage build that broke Modal provider. Remove the multi-stage offload-builder stage (which caused `Multistage Dockerfiles are not supported yet` assertion failure) and instead install Rust, compile offload, and clean up in a single RUN layer. Extract dependency installation into `scripts/post_file_setup.sh` shared between the Dockerfile and offload's new `post_patch_cmd` (v0.9.4), keeping both environments in sync. Upgrade offload from 0.9.2 to 0.9.4 across Dockerfile, CI, and all offload configs.
