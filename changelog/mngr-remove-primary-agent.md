Spec draft: remove the "primary chat" concept from the minds app. The main workspace agent becomes a non-chat `system-services` agent that runs only the bootstrap service manager; a separate `assistant` chat agent is launched for the user (auto-created on first run with `/welcome`). The user's chosen workspace name applies to the mngr host, not to any agent. Removes the agent-rename step in the mngr_imbue_cloud create path.
