Approve native-build scripts for `@firebase/util`, `dtrace-provider`, `electron`, and `protobufjs` in `apps/minds/pnpm-workspace.yaml` so `just minds-start` no longer fails on pnpm 11 (which treats unapproved build scripts as a hard error during the `pnpm start` deps-status check).
