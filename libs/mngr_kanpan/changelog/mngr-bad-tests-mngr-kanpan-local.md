Strengthened and de-duplicated the mngr-kanpan test suite (no user-facing behavior change). Replaced weak/tautological assertions with checks that pin real behavior, added missing branch coverage, and removed tests that only echoed constructor args or exercised framework code:

- Fetcher: parallel-source tests now assert field values round-trip per source (no cross-contamination); added a partial-failure test (one source raises, one succeeds); cache tests now assert serialized content and that the swallowed-error path leaves no file. Extracted a shared `FakeDataSource` into `mock_data_source_test.py`, replacing three ad-hoc fakes.
- TUI: board-widget tests now assert rendered content, section grouping, mark glyphs, and muted styling rather than only the row count; refresh/focus tests assert focus restoration and the loading placeholder; footer tests assert exact strings; removed a duplicate command-loader test and parametrized state-attr coverage.
- Data sources: shell timeout test asserts the timeout message and uses a unique sleep constant; added github nonzero-exit-with-partial-errors, git_info non-integer-stdout, and repo_paths HTTPS/`.git`/non-github-host coverage; github pagination warning asserts distinctive substrings; migrated git_info error-branch tests off `unittest.mock` to concrete fakes (ratchet tightened).
- Data types / data source / plugin / CLI: parametrized CI display across all statuses, broadened `merge_with` coverage, isolated the unknown-key deserialization case, added a non-bool `muted` coercion case, moved a `DiscoveredAgent` factory into `testing.py`, and tightened the invalid-CEL and filter-passthrough CLI tests.
