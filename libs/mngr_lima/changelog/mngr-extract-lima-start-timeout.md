- `mngr_lima`: bump `limactl_start_new` default timeout from `600s` to `1800s` to fit the FCT pilot template's heavier `extra_provision_command` chain (apt+npm+curl+uv steps plus `playwright install --with-deps chromium`, which downloads a ~200MB browser on a fresh VM with cold caches). Empirically observed on 2026-05-29: a `mngr create ... .lima` invocation against the FCT `pilot` branch made progress through ssh + user session + ControlMaster + systemd + containerd + guest agent + "Waiting for boot scripts must have finished", then was SIGTERMed at exactly 600s with exit code -15. The 600s default predates the FCT pilot's heavier provisioning weight and was generous on lighter templates.
