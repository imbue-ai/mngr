- mngr_forward: when the resolver cannot route a subdomain request to an agent, log a diagnostic warning (`_warn_resolver_miss`) that distinguishes the two failure modes -- "agent not in the resolver's known set" (host-side discovery hasn't seen it) vs "agent known but the requested service URL is missing" (the VM-side app-watcher never published the URL). The warning is throttled per-agent (30s) so the 1Hz auto-refresh on the 503 retry page does not flood the log.
