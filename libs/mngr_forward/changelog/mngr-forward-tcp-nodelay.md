The per-host SSH tunnel now disables Nagle's algorithm (sets `TCP_NODELAY`) on its transport socket. The tunnel multiplexes bulk data downstream with tiny interactive frames upstream; without this a lone small packet on an otherwise-quiet connection could sit behind delayed-ACK for up to ~40ms before shipping. This tightens interactive latency for anything that rides the tunnel (e.g. streamed-browser input).
