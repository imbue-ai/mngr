Registered a new shared pytest marker, `witnesses(coordinate, partial=None)`, in the centralized marker list. Tests anywhere in the monorepo can now declare which behavioral-spec unit they verify (by its coordinate, e.g. `authentication.fresh-code`), with `partial=` noting what the test does not cover. See the `behavioral-specs` skill for the coordinate system.
