Fix `test_create_modal_passthrough_agent_args` by passing `--type claude` explicitly. Since the default agent type is no longer hardcoded to `claude`, the e2e test profile has no default configured and the bare `mngr create --provider modal` form fails with "No agent type provided". This matches the existing pattern in `test_create_basic.py` where modal/agent tests pass `--type` explicitly.
