- Fixed the release-marked e2e test `test_destroy_all_via_stdin` (tutorial block `mngr list --ids | mngr destroy - --force`): added `@pytest.mark.timeout(300)` so it no longer hits the default 10s pytest timeout while provisioning two local command agents, and removed the superfluous `@pytest.mark.modal` mark since the test only provisions local agents and never creates a Modal environment (the resource guard correctly flagged the mark as never-invoked). Also strengthened the assertions to confirm the stdin pipe destroyed both agents.
