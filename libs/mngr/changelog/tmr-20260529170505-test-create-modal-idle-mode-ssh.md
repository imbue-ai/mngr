- Fixed the e2e tutorial test fixture so `mngr create` commands that omit `--type` work again. The source-coded `--type claude` default was removed (the installer now writes `[commands.create] type` to user settings), which broke every tutorial test that creates an agent without an explicit type. The `e2e` fixture now writes the same `[commands.create] type = "claude"` default the installer would, mirroring a real user's configured environment.
- Strengthened `test_create_modal_idle_mode_ssh`: it now verifies via `mngr list --format json` that the created agent actually resolves to ssh idle mode (`idle_mode == "ssh"` with activity sources `SSH/CREATE/START/BOOT`), rather than only checking that the command exits 0.
