- e2e tests: the `test_create_with_no_ensure_clean` release test now scopes its verification listing to `mngr list --provider local` (the agent runs on the local provider, the tutorial's default) and drops the `@pytest.mark.modal` mark. The default all-provider listing fanned out to Modal via in-subprocess gRPC, which the resource guard cannot observe, so the modal mark failed its "never invoked" check. The test no longer depends on Modal.
