- e2e release test `test_create_modal_build_args`: pass an explicit `--type claude` to `mngr create` (the agent-type default moved into user config, so the tutorial's implied default is no longer assumed by the test fixture) and verify the `image=python:3.12` build arg actually took effect by reading back `python --version` from the host (expecting Python 3.12), rather than only asserting the create command exited 0.
