Added a `@pytest.mark.timeout(120)` marker to the `test_connect_explicit_host_and_provider` e2e tutorial test. The provider-qualified `agent@host.provider` connect spec drives Modal provider setup, which together with mngr's subprocess startup cost exceeds the default 10s per-test timeout, so the test was timing out despite the command behaving correctly. This matches the timeout already carried by the sibling `test_connect_explicit_host` test.
