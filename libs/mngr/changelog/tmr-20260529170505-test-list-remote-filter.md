- The `test_list_remote_filter` e2e tutorial test now creates a real remote (Modal) agent before running `mngr list --remote`, then asserts the agent appears under `--remote` and is excluded from `--local`. Previously the test ran `mngr list --remote` against an empty environment, which only touched Modal via the in-subprocess SDK (invisible to the resource guard) and so failed the `@pytest.mark.modal` superfluous-mark check while verifying nothing about the filter.
