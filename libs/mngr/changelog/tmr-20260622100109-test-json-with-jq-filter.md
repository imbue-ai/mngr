Fixed the `test_json_with_jq_filter` release test (for the "combine json with jq" tutorial block) timing out under the default 10s per-test cap. The test runs `mngr list --format json`, whose Modal discovery is slow when credentials are present; it now uses `@pytest.mark.timeout(180)`, matching the existing `test_list_format_jsonl_recap` pattern, with a NOTE explaining why it is not marked `@pytest.mark.modal`.
