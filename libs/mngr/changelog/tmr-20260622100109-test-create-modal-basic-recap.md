Fixed the `test_create_modal_basic_recap` e2e tutorial test so its verification listing scopes to `--provider modal`. A bare `mngr list` fans out to every installed provider plugin and aborts (default `--on-error abort`) when an installed-but-unconfigured provider (e.g. the AWS plugin without credentials) fails during discovery, which is unrelated to the Modal create this test covers. Scoping the listing to the modal provider matches the rest of the Modal create tests.
