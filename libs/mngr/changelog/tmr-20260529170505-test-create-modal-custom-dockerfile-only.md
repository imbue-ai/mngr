- Fixed the e2e tutorial fixture (`libs/mngr/imbue/mngr/e2e/tutorial` via `e2e/conftest.py`) to configure a default agent type (`[commands.create] type = "claude"`), mirroring an onboarded user. The tutorial's remote `mngr create` examples omit `--type` and relied on this default, so without it every such test failed with "No agent type provided". This unblocks the modal and docker create tests.
- Strengthened `test_create_modal_custom_dockerfile_only` to prove the custom Dockerfile was actually built and used: its Dockerfile now bakes a sentinel file via a `RUN` step, and the test reads it back with `mngr exec` after creation instead of only asserting the create command exited 0.
