Fix `test_create_modal_volume` e2e test: configure a default agent type and a sleep-based command for the `command` agent type before invoking the tutorial's `mngr create my-task --provider modal -b volume=my-data:/data`. The tutorial assumes the user has set a default agent type during install (`mngr extras config`), but the e2e fixture starts from a fresh profile, so the test was failing with "No agent type provided" before this fix.
