- Fixed the `EXEC --cwd` tutorial e2e test (`test_exec_cwd` in `libs/mngr/imbue/mngr/e2e/tutorial/test_exec.py`): added a `@pytest.mark.timeout(120)` override so the default 10s per-test timeout no longer kills the test while the agent is being created, and dropped the superfluous `@pytest.mark.modal` mark (the test creates a local command agent and runs `mngr exec` locally, so modal is never invoked and the resource guard rejected the mark).
- Strengthened the assertion to require `pwd` to print exactly `/tmp` on its own line (the agent's work_dir is itself a temp dir under `/tmp`, so the previous substring check would have passed even if `--cwd` were ignored), and added a companion test `test_exec_cwd_defaults_to_work_dir` that covers the block's documented default ("by default, commands are run in the agent's work_dir") by confirming `mngr exec my-task "pwd"` without `--cwd` reports the work_dir rather than `/tmp`.
