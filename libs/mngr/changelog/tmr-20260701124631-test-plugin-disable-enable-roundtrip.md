Fixed the `test_plugin_disable_enable_roundtrip` e2e test so it exercises the plugin enable/disable roundtrip under the pytest config guard. The test now toggles the claude plugin at `--scope local`, which writes the enabled flag to the `settings.local.toml` the e2e fixture opts into pytest runs; a default project-scope write would have created a fresh `settings.toml` without the opt-in, causing the follow-up `mngr plugin list` to fail the pytest config guard.
