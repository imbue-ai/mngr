- e2e tests: fixed `test_create_short_forms` so it passes. Added `@pytest.mark.timeout(120)` (the file was the only `test_create_*` e2e file missing an explicit per-test timeout, so it hit the global 10s default and timed out during its second local agent creation). Removed the superfluous `@pytest.mark.modal` mark: the test only creates local agents and runs `mngr list`, whose Modal contact goes through the in-subprocess Python SDK, which the in-process SDK resource guard cannot observe; the mark therefore tripped the guard's `NEVER_INVOKED` check.
