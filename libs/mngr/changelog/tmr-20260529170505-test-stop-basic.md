- Fixed the `test_stop_basic` tutorial e2e test: removed the superfluous `@pytest.mark.modal` mark, since `mngr stop my-task` for a local command agent never queries the Modal provider (only `mngr list`-style commands enumerate all providers). The resource guard was failing the otherwise-passing test for declaring a Modal mark it never exercised. Also strengthened the test to verify, via `mngr list --provider local --format json`, that the agent actually transitions from `RUNNING` to `STOPPED` rather than only checking the command's exit code.
