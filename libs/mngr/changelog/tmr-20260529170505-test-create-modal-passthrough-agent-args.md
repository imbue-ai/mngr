- Fixed the release e2e test `test_create_modal_passthrough_agent_args` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_modal.py`), which was failing with "No agent type provided" because the isolated test environment configures no default agent type. The test now passes `--type claude` explicitly (claude being the type the `-- --dangerously-skip-permissions --append-system-prompt ...` passthrough flags target).
- Strengthened the same test to verify the passthrough args actually reach the agent's launch command, by asserting the persisted `command` field returned by `mngr list --format json` contains the passed-through claude flags and system-prompt value.
