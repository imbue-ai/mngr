Fixed the `test_list_jsonl_jq_stream` LABELS tutorial e2e test so it scopes `mngr list` to `--provider local`. The surrounding test environment leaves remote providers enabled but uncredentialed, so a bare `mngr list` aborted discovery (and JSONL batch mode emitted no agent lines), which broke the streaming jq filter assertion. The test now lists only local agents, dropped the no-longer-applicable `@pytest.mark.rsync` mark, and additionally parses the jq-selected JSON object to confirm it is the high-priority agent.
