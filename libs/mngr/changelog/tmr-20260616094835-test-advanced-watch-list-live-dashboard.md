Fixed the `test_advanced_watch_list_live_dashboard` e2e tutorial test. The dashboard-content check now scopes `mngr list` to `--provider local` so it no longer fails when remote providers (e.g. aws) are enabled but unconfigured in the test environment. Also removed the spurious `@pytest.mark.rsync` mark, since the local-only test never invokes rsync and the resource guard flagged it as never-invoked.
