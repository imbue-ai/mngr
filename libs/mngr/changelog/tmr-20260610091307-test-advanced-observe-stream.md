Strengthened the `mngr observe --discovery-only` e2e tutorial test to verify the documented streaming contract: it now asserts the stream re-emits more than one full discovery snapshot over its window (rather than behaving as a one-shot dump), checks that every emitted event carries the standard envelope (`source`, `type`, `event_id`, `timestamp`), and confirms `event_id`s are unique across the stream.
