- mngr: drop the per-load `Agent X has type 'Y' which is no longer registered; loading with fallback class Z so existing commands keep working.` warning from `Host._load_agent_from_dir`. The orphan-fallback behavior is unchanged; the agent's lifecycle state still surfaces as `RUNNING_UNKNOWN_AGENT_TYPE` in `mngr list`, which is the canonical user-facing signal. The warning duplicated that signal noisily on every `mngr list`/`mngr forward`/`mngr message` invocation in workflows where the workspace's `[agent_types.X]` block is invisible from cwd (e.g. the laptop-side cwd=$HOME paths minds.app drives).
