Fixed the `test_snapshot_create_all_via_stdin` release test for the "snapshot all agents' hosts" tutorial block. The shared modal-agent setup helper now passes an explicit `--type command` (the isolated e2e environment has no default agent type) and runs `sleep` to keep the host alive, and the test is marked `@pytest.mark.rsync` since modal host creation transfers files via rsync. The test also now verifies the snapshot is actually created and listable rather than only checking the exit code.
