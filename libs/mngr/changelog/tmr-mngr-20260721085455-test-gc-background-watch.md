Strengthened the `test_gc_background_watch` e2e tutorial test so it verifies the real effects its scope describes: the `mngr config set commands.destroy.gc false` step now asserts the setting was actually written (not just that the command exited 0), and the `watch -n60 mngr gc` step now asserts `timeout` returned exit code 124 -- confirming `watch` genuinely started and was running gc in the background until the cap fired -- instead of masking the exit code with `|| true`.
