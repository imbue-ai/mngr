- Fixed the `test_recipe_launch_check_cleanup` e2e tutorial test (under `libs/mngr/imbue/mngr/e2e/tutorial/`) so it passes as a release test. The test now has an appropriate `@pytest.mark.timeout`, asserts the real behavior of the `mngr transcript` and `mngr conn` steps against the lightweight local command-agent substitute (a command agent produces no common transcript, and `mngr conn`'s interactive tmux attach cannot complete in a headless test runner), and no longer carries a superfluous `@pytest.mark.modal` (it exercises only local agents). It also now verifies concrete effects: the created agent appears in `mngr list`, and after `mngr destroy --remove-created-branch` both the agent and its `mngr/<name>` branch are gone.
