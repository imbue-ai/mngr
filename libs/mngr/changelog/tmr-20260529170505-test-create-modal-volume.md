Fixed the `test_create_modal_volume` e2e tutorial test, which mounts a persistent Modal volume (`-b volume=my-data:/data`). The test previously relied on a user-configured default agent type, which the isolated e2e fixture does not provide, so it failed with "No agent type provided". It now pins `--type command` with a long-lived `sleep` command (matching the other command-typed Modal tests) and verifies that the volume is actually mounted read-write at `/data` on the agent's host.
