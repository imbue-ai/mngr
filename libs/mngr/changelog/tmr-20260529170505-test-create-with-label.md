- Removed the now-stale `@pytest.mark.modal` from `tutorial/test_basic.py::test_create_with_label`. The test creates a local command agent and runs `mngr list`; since `mngr list` no longer auto-creates a Modal environment (it no longer invokes the Modal CLI), the resource guard flagged the Modal mark as never-exercised. The test still verifies that `--label` and `--host-label` values appear in the JSON output.
