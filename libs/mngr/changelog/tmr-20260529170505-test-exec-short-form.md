Fixed the `test_exec_short_form` tutorial e2e test, which was failing the resource guard: it carried a superfluous `@pytest.mark.modal` mark even though it only creates a local `command` agent and never invokes Modal. Removed the mark and strengthened the assertions to verify the short-form `mngr x` actually runs the command on the agent's host (by checking the `git status` output) and that it behaves identically to the long-form `mngr exec`.
