Fixed the `test_create_modal_idle_timeout_120` e2e tutorial test, which failed because the executed command omitted an agent type (the tutorial relies on a configured default that the isolated e2e environment does not set). The test now substitutes `--type command -- sleep` to make the command runnable, matching the convention used elsewhere in the modal/basic e2e tests, and additionally verifies the concrete effect by asserting that the created agent reports `idle_timeout_seconds == 120` via `mngr list --format json`.
