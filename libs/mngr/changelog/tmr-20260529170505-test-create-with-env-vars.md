- Fixed the `test_create_with_env_vars` tutorial e2e test: added an explicit `@pytest.mark.timeout` so the local `mngr create` has time to finish (it was hitting the 10s default), and removed the incorrect `@pytest.mark.modal` mark since the command uses the local provider. The test now also verifies the agent actually received the `--env` values by reading them back with `mngr exec ... printenv`.
