- Fixed the `test_exec_timeout` release e2e test (under `libs/mngr/imbue/mngr/e2e/tutorial/test_exec.py`): added the `@pytest.mark.timeout(120)` marker so the test is not killed by the default 10s pytest timeout while creating an agent, and removed the superfluous `@pytest.mark.modal` marker since the test creates a local command agent and never invokes Modal (the resource guard was failing the test for the unused mark).
- Strengthened `test_exec_timeout` to assert on the command's actual stdout, and added a new `test_exec_timeout_exceeded` test (sharing the same tutorial block) that verifies `mngr exec --timeout` actually terminates a command that runs longer than the timeout and surfaces a non-zero exit.
