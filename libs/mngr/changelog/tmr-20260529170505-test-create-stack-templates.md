Fixed and strengthened the e2e tutorial test `test_create_stack_templates`, which exercises stacking multiple `mngr create --template` presets. The test now uses the correct `@pytest.mark.tmux` mark (and drops the inaccurate `modal` mark, since the substituted templates run against the local provider), defines its template stubs with valid config fields, and verifies the documented "later templates override earlier ones" behavior by inspecting the created agent's environment.
