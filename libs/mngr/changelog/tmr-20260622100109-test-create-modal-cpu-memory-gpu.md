Added an unhappy-path release test for the Modal resource build args (`mngr create --provider modal -b cpu=... -b memory=... -b gpu=...`) tutorial block: it verifies that a malformed resource value (a non-numeric `cpu`) is rejected with a clear "Invalid build argument" error and leaves no agent behind, complementing the existing happy-path test that checks the recorded cpu/memory resources.
