Removed the spurious `@pytest.mark.modal` from the `test_create_with_source_path_no_git` e2e release test. The test creates a purely local command agent from a non-git folder source and never invokes Modal, so the resource guard's superfluous-mark check (NEVER_INVOKED) was failing it. The remaining `tmux` and `rsync` marks accurately reflect the resources the test actually exercises.
