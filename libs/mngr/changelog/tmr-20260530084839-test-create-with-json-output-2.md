- Fixed the `test_create_with_json_output` e2e release test: gave it an explicit `@pytest.mark.timeout(120)` (the default 10s function timeout was too tight for an agent create plus a `mngr list` discovery pass) and removed the `@pytest.mark.modal` mark, which the resource guard cannot satisfy for this test (it creates a local agent and only performs read-only Modal discovery via the in-subprocess Python SDK, never the `modal` CLI binary). Strengthened the test to cross-check that the `agent_id`/`host_id` returned by `mngr create --format json` match the agent reported by `mngr list`.
- Added a `test_create_with_quiet_output` e2e test covering the tutorial's `--quiet` note: `mngr create --quiet` suppresses all console output while still creating the agent.
