Fixed the `test_advanced_create_reuse_modal` e2e tutorial test, which was failing because `mngr create` now requires an explicit agent type (no hardcoded "claude" default) and the e2e environment configures none. The test now passes `--type command` (with a long `sleep` command) to exercise the `--reuse` idempotency path without incurring Modal Claude startup time. Also strengthened the test to verify idempotency by running `create --reuse` twice and asserting no duplicate agent is created.
