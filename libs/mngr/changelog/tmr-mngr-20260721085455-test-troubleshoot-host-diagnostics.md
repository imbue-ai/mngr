- Strengthened the TROUBLESHOOTING tutorial e2e test for `mngr exec` host diagnostics: the piped `cat /var/log/syslog | tail -20` block now verifies that mngr actually forwarded the piped command to the agent's host (asserting no agent-resolution failure) instead of swallowing every outcome with `|| true`. The host command is still allowed to fail (syslog may not exist), but a mngr-level forwarding regression is now caught.
