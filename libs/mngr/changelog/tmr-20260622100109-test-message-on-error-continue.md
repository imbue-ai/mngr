Fixed the `test_message_on_error_continue` e2e tutorial test: added a `@pytest.mark.timeout(180)` marker so it no longer trips the default 10s func-only timeout while creating agents, and removed the incorrect `rsync` and `modal` resource marks (message delivery to a local agent only exercises tmux). Also strengthened the test to message multiple agents and assert that the message is actually delivered to each.
