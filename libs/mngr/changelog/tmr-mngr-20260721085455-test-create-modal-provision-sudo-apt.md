Made the `test_create_modal_provision_sudo_apt` e2e release test robust against the transient window right after `mngr create --no-connect`, where a fresh `mngr exec` against the newly-created Modal host can briefly fail to find the agent. The verification exec now retries via the shared `_exec_until_reachable` helper (matching the sibling upload/provision tests), and the per-test timeout was bumped from 240s to 300s to accommodate the extra round-trips.
