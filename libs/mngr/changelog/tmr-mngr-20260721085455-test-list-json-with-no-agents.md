Fixed the `test_list_json_with_no_agents` e2e tutorial test so it passes under the default per-test timeout: added an explicit `@pytest.mark.timeout(60)` (the `mngr list --format json` provider-discovery path routinely exceeds the default 10s) and removed the spurious `@pytest.mark.modal` mark, matching the sibling fresh-environment `mngr list` tests that never invoke the `modal` CLI across the e2e subprocess boundary.
