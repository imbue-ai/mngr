- Test only: hardened the `test_create_duplicate_name_fails` e2e release test. It previously inherited the global 10s pytest timeout, which was too short for two `mngr create` invocations (plus first-run ttyd install) and caused spurious timeout failures; it now uses `@pytest.mark.timeout(120)` like sibling create tests. The test also now verifies the duplicate-name error message and confirms that exactly one `my-task` agent survives the failed duplicate create.
