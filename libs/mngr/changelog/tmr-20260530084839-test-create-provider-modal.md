Fixed the `test_create_provider_modal` e2e release test: the Modal create command now passes an explicit `--type command` agent type (with a trailing command) so it runs in the e2e environment, which configures no default agent type. The test also now verifies the created agent is discoverable via `mngr list` and reports the `modal` provider as its host owner, rather than only checking the command's exit code.
