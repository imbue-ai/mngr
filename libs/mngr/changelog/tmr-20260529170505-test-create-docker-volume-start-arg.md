- Fixed the `test_create_docker_volume_start_arg` release test in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_docker.py`, which failed with "No agent type provided" because the executed `mngr create` command omitted an agent type. The command now passes `--type command -- sleep <n>` (matching the convention used by every other tutorial e2e test) while keeping the tutorial block unchanged.
- The test now also verifies the volume start arg actually takes effect: it writes a marker file into the host volume source before create, then reads it back from inside the container via `mngr exec`, confirming the `-v host:container` bind mount is shared with the agent.
