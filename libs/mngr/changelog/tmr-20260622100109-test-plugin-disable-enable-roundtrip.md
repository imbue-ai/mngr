Fixed the `test_plugin_disable_enable_roundtrip` e2e release test so the plugin disable/enable roundtrip is exercised at the user config scope. Toggling at the default project scope wrote a fresh project `settings.toml` that lacked the `is_allowed_in_pytest` opt-in, causing the next `mngr` command to be refused by the pytest config guard. The user-scope profile `settings.toml` is already opted in, so the roundtrip now runs as written and verifies that re-enabling restores the full plugin list (including version/description metadata).
