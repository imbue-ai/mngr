- Fixed the `test_destroy_specific` tutorial e2e test (covering the `mngr destroy my-task` block): added a `@pytest.mark.timeout(120)` so the Modal create/destroy has enough time (it previously inherited the 10s global timeout and always timed out), and removed the inaccurate `@pytest.mark.modal` marker since the test only creates a local command agent and never invokes the `modal` binary.
- Strengthened the same test to actually exercise destruction: it now stops the agent first (a running agent is refused without `--force`), confirms the interactive prompt, asserts the agent is reported as destroyed, and verifies via `mngr list` that it no longer exists.
