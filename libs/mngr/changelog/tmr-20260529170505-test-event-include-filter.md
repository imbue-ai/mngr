- Fixed the `test_event_include_filter` e2e tutorial test: added a `@pytest.mark.timeout(120)` marker (it was falling back to the global 10s function-body timeout, too short for a test that provisions a host and reads events) and removed the superfluous `@pytest.mark.modal` marker (the test creates a local command agent and reads events locally, so it never invokes Modal).
- Added `test_event_include_filter_invalid_cel`, an unhappy-path test covering the same tutorial block: it verifies that `mngr event --include` rejects a malformed CEL expression with a clear "Invalid include filter expression" error and a non-zero exit code.
