Improved the `test_create_modal_provision_pip_install` e2e test so it actually exercises the tutorial's headline behavior: instead of having `--extra-provision-command` echo a marker file, it now installs a tiny dependency-free Python package (`six`) during provisioning and verifies the package is importable on the host. This more faithfully matches the tutorial's `pip install numpy pandas` example while staying fast and avoiding flakiness.
