Test-only: fixed the `test_create_with_pass_env` e2e release test. Scoped its `mngr list` verification to `--provider local` so it no longer fails when an unrelated external provider (e.g. AWS) is enabled without credentials in the test environment, and removed the inaccurate `@pytest.mark.rsync` mark (a local git-repo agent uses git-worktree transfer with a clean working tree, so rsync is never invoked).
