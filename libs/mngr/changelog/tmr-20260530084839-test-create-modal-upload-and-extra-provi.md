Fixed the `test_create_modal_upload_and_extra_provision_command` release test, which had started failing because the isolated e2e environment configures no default `commands.create.type`, so `mngr create` errored with "No agent type provided". The test now passes `--type claude` explicitly (matching the tutorial's assumed default). It also now verifies the two features it covers: that `--upload-file` lands the file at its target path on the remote host, and that `--extra-provision-command` actually runs during provisioning.
