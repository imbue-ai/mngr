Fixed the `test_advanced_watch_list_live_dashboard` e2e tutorial test. It was marked `@pytest.mark.modal` but `mngr list` only reaches Modal through the in-process SDK, which the resource guard cannot track across the `mngr` subprocess boundary, so the mark always failed the "never invoked modal" check. Removed the superfluous mark and added a direct `mngr list` assertion to verify the dashboard actually renders.
