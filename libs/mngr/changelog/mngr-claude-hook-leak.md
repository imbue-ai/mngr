The `__extend` config operator now merges dicts **recursively**. A nested `key__extend` inside an `__extend` value merges deeper (extending `base[key]`) instead of replacing it, while a nested bare `key` still replaces at that level (preserving its siblings). This is backward-compatible: any value that nests no inner `__extend` marker merges exactly as the previous one-level operator did, so no existing config changes behavior.

The deferred-`__extend` carveout (markers preserved verbatim through config-load for later resolution against a concrete runtime base) now covers a small registry of paths: the existing `create_templates.<name>` options (resolved at `mngr create` time) plus any path under `agent_types.<name>.settings_overrides` (resolved at Claude provisioning time against the home `settings.json`). This lets a claude agent type's `settings_overrides` merge onto your home Claude settings with config-consistent semantics -- bare keys assign (with the narrowing guard), `key__extend` merges -- rather than a blanket deep-merge.

A field marked as a settings *patch* (e.g. a claude agent type's `settings_overrides`) now **accumulates** across config scopes (user < project < local) and across `parent_type` inheritance, rather than a higher/child scope replacing the entire lower/parent value. The per-scope patches are combined associatively per-key (preserving and combining `__extend` markers, with a higher bare key winning over a lower marker), so non-overlapping keys from every scope survive and same-key `__extend`s accumulate. The narrowing guard against the provision base is now threaded recursively through the merge, so a bare assign that drops a non-empty aggregate is caught **at any depth** -- including a bare key nested inside an `__extend` value.

New config-merge operator `key__assign`: an explicit "assign without the narrowing warning". It replaces the value from the layer below exactly like a bare `key`, but records no narrowing violation -- the per-key opt-out to the assign-narrowing guard (coexisting with the global `allow_settings_key_assignment_narrowing` flag). Within a single layer, resolution is now two-phase and order-independent: assigns (bare and `__assign`) apply first, then `__extend` stacks on top, so `key__assign` + `key__extend` means "reset-without-warning, then add". A bare `key` and `key__assign` for the same field in the same layer is a contradictory double-assign and raises a clear `ConfigParseError`.

New `Static*` aggregate markers (`StaticTuple`, `StaticList`, `StaticDict`): wrap an aggregate value to mark it **atomic**, so replacing it from a higher layer is a value-set rather than aggregate narrowing and is exempt from the narrowing guard. mngr's existing `StringDerivedTuple` / `ScalarTuple` are now `StaticTuple` subclasses, so the long-standing "a string-form `cli_args` is a whole value, not narrowing" behavior is a reusable concept rather than a special case.

The two settings-patch primitives (`combine_patches` for cross-layer combine and the provision-time narrowing fold) are unified into `merge(lower, higher) -> (patch, narrowings)` plus `finalize(patch) -> dict`. `merge` is the associative four-rule combine *and* the recursive narrowing tracker (honoring `__assign` / `Static*` suppression; never raising for narrowing -- the caller decides what to do with the returned paths); `finalize` resolves any leftover `__extend` against an empty base into a marker-free dict.

The generic, model-free part of this merge algebra (the `__extend` / `__assign` operators, the `Static*` markers, `combine_patches`, `merge` / `finalize`, and the `would_assignment_narrow` predicate) has been extracted into a new standalone, dependency-free `overlay` library; mngr now depends on it and imports these from `imbue.overlay`. This is a pure refactor with no behavior change. mngr's `ConfigParseError` now also subclasses `overlay`'s `OverlayError`, so existing `except ConfigParseError` handling is preserved while raw `OverlayError`s from the algebra are caught by the same config-load / merge boundaries.

`resolve_extends` now preserves a deferred `key__assign` inside `agent_types.<name>.settings_overrides` (when the config-load base has no value for it) rather than collapsing it to a bare assign. This carries the no-warn intent through to the provision-time fold, where it suppresses the narrowing guard as written. Scoped to `settings_overrides`; `create_templates` is intentionally excluded because its consumer (`apply_create_template`) reads only `__extend`.

(Developer-internal, no user-facing behavior change) Added an exploratory prototype (`config/overlay_merge_prototype.py`) and a property test proving that `AgentTypeConfig.merge_with` can be reproduced by a serialize -> pre-process -> overlay-merge -> reparse pipeline built on the `overlay` node algebra, with no field-by-field pydantic copy. Not wired into any production path; it exists to de-risk a future migration of the config merge onto `overlay`.

(Developer-internal, no user-facing behavior change) Extended the above prototype up to the **top-level** `MngrConfig.merge_with` (`config/overlay_merge_mngr_prototype.py` plus a property test). It reproduces the whole-config merge through the same overlay pipeline -- handling the top-level `parse_config` None-padding (treat a `None` scalar as unset, reproducing `_assign_scalar`), the per-key container-additive merge of `agent_types` / `providers` / `plugins` / `commands` / `create_templates` (via recursive `__extend` marking of both the container field and each entry, so nested `settings_overrides` still accumulates and container-entry subclasses round-trip), the `retry` / `logging` sub-model merges, and the defaults-application timing (defaults applied only by the final `model_validate`, reconciled on both sides of the equality). Still not wired into any production path; further de-risks the eventual migration.

(Developer-internal, no user-facing behavior change) Completed the prototype across the **last** merge axis: the `parent_type` inheritance path `_apply_custom_overrides_to_parent_config` (`config/overlay_merge_parent_type_prototype.py` plus a property test). This is the class-switching variant -- the result is constructed as the *parent's* concrete config class even though it applies the child's fields -- so it reproduces the function by dropping the routing-metadata fields (`parent_type` / `plugin`) from the child's dump, accumulating `settings_overrides` across the parent/child boundary, and re-parsing the merged dict into `type(parent)` (so a base-class child folded onto a `ClaudeAgentConfig` parent yields a `ClaudeAgentConfig`, with subclass-only fields supplied by the parent). With this, the entire config merge surface (cross-scope `AgentTypeConfig` and `MngrConfig` `merge_with`, plus `parent_type` inheritance) is proven reproducible through the single overlay pipeline. Still not wired into any production path.

Fixed a latent crash in `MngrConfig.merge_with`: it called `.merge_with` on `self.retry` / `self.logging` without checking they were set, raising `AttributeError` when the base config's `retry`/`logging` was `None` (as a `model_construct`'d layer can be) and the override set them. It now takes the override outright when the base sub-model is unset. Masked in production because the merge's base operand is always the loader's defaulted accumulator, but a real latent footgun.

(No user-facing behavior change) `AgentTypeConfig.merge_with` now computes its result through the overlay node algebra (the proven serialize -> pre-process -> overlay-merge -> reparse pipeline) instead of a field-by-field pydantic copy. The proof-of-approach prototype is promoted into a production module (`config/overlay_merge.py`, `merge_models_via_overlay`): bare fields assign-by-default (the override's set fields win, the base carries through), `SettingsPatchField` fields (a claude agent type's `settings_overrides`) accumulate via `__extend` (the `combine_patches` branch), and the merged dict re-parses into the concrete `type(self)` so a `ClaudeAgentConfig` stays a `ClaudeAgentConfig` with subclass-only fields intact. This is a pure refactor of *how* the merge is computed -- the result is identical to before, guarded by a property test (`config/overlay_merge_test.py`) that asserts the new path equals the frozen old field-by-field logic over a diverse corpus (scalars, `cli_args` string/list/unset, `settings_overrides` bare/`__extend`/nested/`__assign`/accumulate, subclass fields, empty/both-empty overrides). The `MngrConfig.merge_with` and `parent_type` inheritance paths are not yet wired (their prototypes remain).
