Made the `test_create_modal_upload_and_extra_provision_command` e2e tutorial test robust to the eventual-consistency window on freshly-created Modal hosts: the two verification `mngr exec` calls now retry via `_exec_until_reachable` (instead of failing on a transient "Agent not found"), and the test timeout was raised from 180s to 300s to accommodate the retries, matching the sibling `test_create_modal_upload_only` test.
