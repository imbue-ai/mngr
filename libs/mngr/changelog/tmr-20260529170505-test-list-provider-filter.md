- Removed the now-incorrect `@pytest.mark.modal` from the e2e test `test_list_provider_filter`. Since `mngr list` stopped auto-creating the Modal environment for read-only commands, `mngr list --provider modal` in a fresh environment never invokes the `modal` CLI (the Modal backend reports itself unavailable and is skipped), so the resource guard failed the test with "marked with @pytest.mark.modal but never invoked modal".
- Strengthened the test to assert the command reports no agents (and that the JSON form returns empty `agents`/`errors`), verifying the provider filter actually runs and returns a clean empty result rather than only checking the exit code.
