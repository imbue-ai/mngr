Test-only: fixed the `test_list_format_json_recap` e2e tutorial test for the JSON output format. It now scopes the listing to `--provider local` (matching the convention used by the other e2e tutorial tests) so it does not abort on unconfigured cloud provider plugins (aws/azure/...) that the monorepo installs but leaves without credentials, and raises the per-test timeout above the 10s default to accommodate `mngr` startup (which loads every installed provider plugin).
