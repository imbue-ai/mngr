Fixed the plugin e2e tests (`test_plugin_disable_affects_create` and `test_plugin_disable_enable_roundtrip`) to toggle the claude plugin at user scope. Disabling at the default project scope created a fresh `.mngr/settings.toml` without the `is_allowed_in_pytest` opt-in, causing every subsequent `mngr` command to fail the pytest config guard for an unrelated reason instead of exercising the disabled-plugin behavior.
