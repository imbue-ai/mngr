- Fixed the `test_create_docker_custom_dockerfile` e2e tutorial test so it can actually create a Docker agent. It now passes an explicit `--type command -- sleep ...` (the isolated test profile has no default agent type, which a real tutorial user would have configured) and writes a `FROM debian:bookworm-slim` Dockerfile instead of `FROM alpine` (mngr installs its required host packages via `apt-get` at container start, which an Alpine base lacks). The create call also gets a longer timeout to accommodate the runtime package install.
