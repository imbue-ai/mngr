Fix the `test_event_default` tutorial e2e test so it no longer carries an incorrect `@pytest.mark.rsync`. The test creates a local command agent in a git repo on the same host, where the default transfer mode is git-worktree (not rsync), so rsync is never invoked and the resource guard rightly flagged the unused mark. Removed the mark and corrected the misleading comment in the shared `_create_my_task` helper.
