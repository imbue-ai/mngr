- Strengthened the `test_create_modal_idle_mode_run` tutorial e2e test: in addition to checking that `mngr create --provider modal --type command --idle-mode run --idle-timeout 60` succeeds, it now verifies the created agent's persisted metadata (agent type, command, idle mode, idle timeout, and Modal provider) via `mngr list --format json` and confirms the long-running command is actually running on the remote host via `mngr exec`.
