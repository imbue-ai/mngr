Hardened the GC background-watch e2e test (`test_gc_background_watch`): it now seeds the pytest opt-in into the project `settings.toml` before `mngr config set` (so the subsequent `mngr gc` can load the freshly written project config), verifies the `commands.destroy.gc = false` setting landed on disk, and lets `watch` run a real `mngr gc` pass instead of being killed before GC starts.
