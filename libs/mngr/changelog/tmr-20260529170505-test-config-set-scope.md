- Strengthened the `test_config_set_scope` e2e test (CONFIGURATION tutorial section): in addition to asserting the `mngr config set headless true --scope user` command succeeds, it now reads the value back from the user scope, confirms the value was not written to the default project scope (scope isolation), and inspects the on-disk user config file to verify `headless = true` was persisted. Added a companion `test_config_set_scope_invalid_scope` test that covers the unhappy path where an unrecognized `--scope` value is rejected. Bumped the per-function timeout to 60s to accommodate the additional command invocations.
