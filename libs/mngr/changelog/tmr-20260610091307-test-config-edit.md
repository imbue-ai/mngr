Fixed the `test_config_edit` e2e tutorial test, which assumed the project-scope `settings.toml` was pre-seeded by the fixture. The fixture deliberately leaves that file absent to exercise genuine first-use behavior, so the test now asserts the project config file does not exist before `mngr config edit`, and that the command creates it from its template (with the `# mngr configuration file` header) before the editor stamps its marker.
