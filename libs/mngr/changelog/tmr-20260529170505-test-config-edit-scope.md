- Strengthened the `test_config_edit_scope` e2e test (`libs/mngr/imbue/mngr/e2e/tutorial/test_config.py`) so it verifies the actual effect of `mngr config edit --scope project` rather than only that the command exits 0: it now cross-checks that the command targets exactly the path reported by `mngr config path --scope project`, that the file does not exist beforehand, and that `config edit` creates it on disk. Added a companion unhappy-path test (`test_config_edit_scope_rejects_invalid_scope`) confirming an unsupported `--scope` value is rejected.
