- Fixed the e2e test `test_create_default` which was failing with a "Test marked with @pytest.mark.modal but never invoked modal" resource-guard violation. The test creates a local agent and never invokes the Modal CLI binary, so the `@pytest.mark.modal` marker was incorrect; removed it.
- Strengthened `test_create_default` assertions to verify the actual behavior more thoroughly: the worktree work_dir differs from the session cwd, the default provider is `local`, the agent state is `RUNNING`/`WAITING`, and the agent is actually executing inside its worktree (verified via `mngr exec my-task pwd`). Added a 60s timeout to accommodate the new exec call.
