- Fixed the release e2e test `test_create_command_agent_runs_post_dash_command_in_agent` (under `libs/mngr/imbue/mngr/e2e/tutorial/`): removed the `@pytest.mark.modal` mark. The test creates a local command-type agent and only runs `mngr list` for discovery, neither of which invokes the Modal CLI binary, so the resource guard correctly reported the mark as never-satisfied. The test exercises the default (local) provider, matching the tutorial block it is tied to.
- Strengthened the same test to also assert the created agent's `type` is `command`, so the `--type command` flag from the tutorial block is verified alongside the post-`--` command string.
