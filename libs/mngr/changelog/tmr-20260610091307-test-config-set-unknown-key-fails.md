Fixed the `test_config_set_unknown_key_fails` e2e tutorial test. It previously read back the project `settings.toml` with `cat` and asserted success, but a rejected `config set` validates before saving and the fixture never seeds that file, so the file does not exist after rejection. The test now asserts the project settings file was never created, which is the strongest proof the invalid key was not persisted.
