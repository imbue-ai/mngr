Fixed the `test_create_duplicate_name_fails` e2e release test. The post-rejection verification now scopes `mngr list` to the local provider (`--provider local --format json`) so it no longer hard-fails when an unrelated provider's discovery errors in the test sandbox (e.g. `aws` with no credentials). Also removed a spurious `@pytest.mark.rsync` mark, since the test never exercises rsync.
