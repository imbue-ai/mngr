Fixed the `test_create_git_mirror_with_existing_branch` e2e tutorial test so it runs to completion. It was missing a `@pytest.mark.timeout` override, so the 10s default function timeout fired before its several `mngr` subprocess calls (a git-mirror create plus four `mngr exec` verifications) could finish; it now allows 180s. It also carried an incorrect `@pytest.mark.rsync` mark, but `--transfer=git-mirror` transfers the repo via git and never invokes rsync, so the resource guard rejected the run; the mark has been removed.
