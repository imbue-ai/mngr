- Fixed the `test_destroy_no_gc` tutorial e2e test so it exercises the real `mngr destroy --force --no-gc` path against a Modal agent. The test previously ran a bare `mngr destroy --no-gc` (no agent), which errored out immediately without contacting Modal and so violated the `@pytest.mark.modal` resource guard; it now creates a Modal agent, destroys it with `--no-gc`, and asserts the agent is gone while the post-destroy garbage collection pass does not run.
