Fixed the `test_control_mngr_via_env` e2e tutorial test (env-var control of mngr config). The test now opts into assign-by-default settings narrowing (the e2e harness pre-sets `commands.create.connect_command` in the local settings layer, which the higher-precedence `MNGR__COMMANDS__CREATE__PROVIDER` env var would otherwise narrow), drops the `modal` resource mark (it forces the `local` provider to stay cheap, so modal is never invoked), and uses a longer timeout to accommodate the local create. Also verifies that the env var actually controlled the provider by asserting the created agent runs on the `local` backend.
