Strengthen `test_help_succeeds` e2e assertions: verify every command name
mentioned in the tutorial comment (create, list, destroy, message, connect,
push, pull, clone) appears in `mngr --help` output, not just `create` and
`list`.
