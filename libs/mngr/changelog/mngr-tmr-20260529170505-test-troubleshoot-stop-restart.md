- Fixed the `test_troubleshoot_stop_restart` e2e test: it created a local command agent and stopped/restarted it, so it never invoked Modal and the `@pytest.mark.modal` resource guard failed it. Removed the superfluous `@pytest.mark.modal` mark and added a `@pytest.mark.timeout(120)` override so the stop+start lifecycle has enough time (the 10s default was too short).
- Strengthened the same test to verify the actual stop/restart effect rather than just exit codes: it now confirms the agent's workload process (`sleep`) is running before the stop, gone while stopped (via `mngr exec --no-start`), and running again after the restart.
