Test-only: made the `mngr list` e2e tests deterministic with respect to which optional provider plugins happen to be importable. The e2e fixture now pins `enabled_backends` to the curated set these tests target (`local` + `modal`, plus `docker` only for `@pytest.mark.docker` tests) instead of leaving every monorepo provider plugin (aws, azure, gcp, ovh, vultr, lima, ...) enabled by default. Previously, when the tests shelled out to the dev-shim `mngr` (which runs the full `uv sync --all-packages` checkout), an installed-but-unconfigured cloud plugin such as AWS raised a discovery error that made `mngr list` exit non-zero -- failing tests locally even though production / CI installs only modal/schedule/claude.

Test-only: added a per-test timeout to `test_list_archived_filter` (its `mngr list --archived` calls run the full provider-discovery path, including an authenticated Modal lookup, and exceed the default 10s per-test timeout that applies when the test is run directly; the release CI lane overrides the timeout globally).

Test-only: strengthened `test_list_archived_filter` to verify the `--archived` filter actually *discriminates*. It now creates a (non-archived) local command agent and asserts the agent is excluded from `mngr list --archived` while remaining visible in an unfiltered listing -- the empty-environment assertions alone could not distinguish a working `has(labels.archived_at)` filter from one that always returns an empty set.
