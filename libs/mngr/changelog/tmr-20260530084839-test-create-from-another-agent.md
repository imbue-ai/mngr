Fixed the `test_create_from_another_agent` e2e release test: added a `@pytest.mark.timeout(180)` override (the global 10s timeout is too short for two sequential agent creations) and removed a superfluous `@pytest.mark.modal` mark (the test creates local agents and never invokes the Modal CLI, so the resource guard flagged the mark as never-invoked). Also strengthened the test to verify that cloning with `--from <agent>` actually carries over the source agent's committed work-directory contents.
