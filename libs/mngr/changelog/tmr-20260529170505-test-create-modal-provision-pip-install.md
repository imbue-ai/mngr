- Fixed the e2e tutorial tests: the shared `e2e` fixture now writes a default `[commands.create] type = "claude"` into the isolated test profile. The source-coded `--type claude` default was removed (the installer now prompts for it), so every bare `mngr create` in the tutorial e2e suite was failing with "No agent type provided"; the fixture now mirrors a post-install user's configuration.
- Strengthened `test_create_modal_provision_pip_install`: it now has the provision command write a marker file and reads it back via `mngr exec`, verifying the `--extra-provision-command` actually runs on the host rather than only checking the create command's exit code.
