- e2e tests: fixed the `--project` label release test (`test_create_with_project_label`) so it runs under the default pytest timeout (added `@pytest.mark.timeout(120)`) and removed its `@pytest.mark.modal` mark, which was unsatisfiable because the test creates a local agent and only reaches Modal via the in-subprocess Python SDK (not observable by the resource guard). Added a happy-path companion test (`test_create_with_default_project_label`) that verifies the default project label matches the current git repo's folder name when `--project` is omitted.
