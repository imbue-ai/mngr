- Fixed the WORKING WITH GIT release test `test_exec_branch_show_current` in `libs/mngr/imbue/mngr/e2e/tutorial/test_git.py`: removed the superfluous `@pytest.mark.modal` mark, since the test exercises a local command-type agent and never invokes Modal (the resource guard fails a passing test that declares an unused resource).
- Strengthened that test to assert the agent reports its auto-created `mngr/my-task` branch rather than only checking the exit code, and added a companion test `test_exec_branch_show_current_after_checkout` that checks out a new branch in the agent's worktree and verifies `git branch --show-current` reflects the shift, covering the tutorial's note that the branch "may have shifted if the agent checked out a new branch".
