Fixed the `test_usage_wait_and_create` e2e tutorial test so its final "confirm the gated create was skipped" check scopes `mngr list` to the local and modal providers. An unscoped listing queries every enabled-by-default backend (including aws/gcp/azure), which now raise `ProviderUnavailableError` when their credentials are not configured, causing `mngr list` to exit 1 on an unrelated provider error and mask the actual check.
