- Fixed the `test_create_docker_start_args` e2e release test, which broke when the source-coded default for `mngr create --type` was removed (the default now comes from user config written by `install.sh`). The test's isolated profile has no default, so it now passes `--type command -- sleep ...` explicitly. This also keeps the container alive for the post-create `mngr exec my-task hostname` readback without needing an API key. The tutorial block is unchanged.
