Test-only: made the `test_create_from_another_agent_source_alias` e2e tutorial test robust to the dev environment registering credential-requiring cloud backends (aws, azure, gcp, ...). Scoped the verification `mngr list` to `--provider local` (matching the other e2e tutorial tests) so it does not enumerate and abort on unconfigured cloud providers, and raised the timeout on the `mngr create --source <agent-name>` step (which forces a full provider scan whose cloud-credential lookups are slow) plus the overall per-test timeout.
