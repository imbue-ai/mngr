- Fix `test_list_filter_by_state` e2e test by adding `@pytest.mark.timeout(120)` (the global 10s pytest timeout was firing during the second `mngr create` call) and removing the superfluous `@pytest.mark.modal` mark (the test creates local command-type agents and never invokes Modal in a way the in-process SDK guard can observe).
- Improve `test_list_filter_by_state` to additionally verify that the `--stopped` filter is keyed on each agent's `state` field (not just its name) and that the non-stopped agent's state is something other than `STOPPED` under the unfiltered listing.
