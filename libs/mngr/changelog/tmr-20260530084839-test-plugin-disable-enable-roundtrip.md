Fixed the `test_plugin_disable_enable_roundtrip` e2e release test so it runs reliably: gave it a generous per-test timeout (the default 10s function timeout is too short for several real `mngr` subprocess invocations) and switched the disable/enable commands to `--scope user`, whose config file is the one the e2e harness opts into pytest runs. Also strengthened the assertions to confirm that disabling a plugin actually blocks its entry point from loading (its version metadata becomes unavailable) and that re-enabling restores it.
