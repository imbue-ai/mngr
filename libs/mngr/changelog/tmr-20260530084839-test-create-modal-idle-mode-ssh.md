Fixed the modal `mngr create` e2e tests (e.g. `test_create_modal_idle_mode_ssh`) that failed with "No agent type provided". The e2e fixture now configures a default `[commands.create] type = "claude"`, mirroring a real user environment where the install flow writes a default agent type. These tests intentionally omit an explicit `--type` to match their tutorial blocks, and type resolution no longer falls back to a built-in default.
