Test-only: fixed the `test_start_dry_run` e2e tutorial test so it actually exercises the start dry-run path. The agent is now stopped before the dry-run (start only plans STOPPED agents, so the previous test -- which left the freshly created agent WAITING -- would never have reported "Would be started"). All `mngr list` invocations are scoped to `--provider local` so the test no longer enumerates remote providers (Modal/Vultr/AWS), keeping it fast and free of unrelated provider warnings. Dropped the superfluous `@pytest.mark.rsync` mark (the test never invokes rsync) and raised its per-command/overall timeouts to absorb command-startup overhead.
