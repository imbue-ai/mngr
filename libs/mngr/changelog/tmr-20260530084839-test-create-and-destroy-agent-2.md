- e2e tests: removed the incorrect `@pytest.mark.modal` from `test_create_and_destroy_agent`. The test creates a purely local `command` agent, and `mngr destroy`/`mngr list` deliberately skip the Modal provider (via `ProviderEmptyError`) rather than creating a Modal environment, so the Modal CLI is never invoked and the resource guard failed with "marked with @pytest.mark.modal but never invoked modal".
- e2e tests: added `test_destroy_nonexistent_agent_with_force_is_graceful`, an unhappy-path test for the same tutorial block verifying that `mngr destroy my-task --force` on a nonexistent agent exits 0 and reports "No agents found to destroy".
