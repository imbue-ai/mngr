- Fixed the `test_create_with_json_output` e2e tutorial test in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_basic.py`: added a `@pytest.mark.timeout(120)` marker (matching the other tutorial e2e test files) so the test is not killed by the default 10s per-test timeout when run with the repository's standard pytest `addopts`, and removed the superfluous `@pytest.mark.modal` marker. The test creates a local-provider agent and never invokes Modal in a guard-detectable way (Modal discovery during `mngr list` happens via the in-process SDK inside the `mngr` subprocess, which the resource-guard monkeypatch -- installed only in the pytest process -- cannot observe), so the mark tripped the superfluous-mark resource guard.
- Added a companion `test_create_with_quiet_output` test covering the `--quiet` half of the same tutorial block, asserting that `--quiet` produces no output on either stream while still creating the agent.
