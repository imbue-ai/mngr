Fixed the release e2e tutorial test `test_event_include_filter` (VIEWING EVENTS section). The test carried `@pytest.mark.rsync`, but the e2e fixture's working directory is a git repo, so `mngr create` for a local agent uses the git-worktree transfer mode (pure git, no rsync binary). The resource guard fails a passing test that declares the `rsync` mark without ever invoking rsync, so the mark made the test fail. Removed the stale `rsync` mark. No production behavior change.
