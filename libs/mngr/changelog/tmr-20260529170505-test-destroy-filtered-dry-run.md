- `mngr destroy` now supports a `--dry-run` flag that reports which agents would be destroyed (respecting `--format human/json/jsonl`) without actually destroying anything, confirming nothing, or running GC. This matches the destroy examples in `mega_tutorial.sh` that already demonstrated `mngr destroy - --force --dry-run`.
- Fixed the `test_destroy_filtered_dry_run` e2e tutorial test: it now creates a real backend-labeled Modal agent so the filter has a concrete target, runs the exact tutorial dry-run command, and verifies the dry-run previews the matching agent without destroying it. Added a `@pytest.mark.timeout` override (the test previously hit the global 10s pytest-timeout during Modal discovery).
