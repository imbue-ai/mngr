Hardened the `test_json_with_jq_filter` e2e tutorial test against flakiness: it exercises the slow `mngr list --format json` provider-discovery path (piped into `jq`), so it now carries the same extended timeouts as its sibling tests (`@pytest.mark.timeout(180)` and a 120s `e2e.run` timeout) to avoid spurious failures when Modal credentials are present. No user-facing behavior change.
