Fixed the `test_message_commit_request` e2e tutorial test (WORKING WITH GIT section): it was marked `@pytest.mark.rsync` but messaging a local agent never invokes rsync (the agent is created with a git worktree, and `mngr msg` only attaches the agent's tmux session). The superfluous mark tripped the resource guard. Removed the mark and documented why, leaving the `tmux` mark in place.
