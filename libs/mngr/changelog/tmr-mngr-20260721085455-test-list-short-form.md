Fixed the `test_list_short_form` e2e tutorial test so it passes under the default per-test timeout: `mngr ls` runs the full provider-discovery path (~20s), so it now carries an explicit `@pytest.mark.timeout(60)`. Also dropped the spurious `@pytest.mark.modal` mark, since a fresh-environment `mngr ls` discovers via the Modal gRPC SDK and never shells out to the `modal` CLI binary (the only Modal usage the resource guard can observe across the subprocess boundary), matching the sibling filter tests.
