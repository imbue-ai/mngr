- Fixed the `test_event_follow` release e2e test, which was failing the resource guard with "marked with @pytest.mark.modal but never invoked modal": removed the superfluous `@pytest.mark.modal` mark (the test exercises only a local command agent, which invokes rsync and tmux but never Modal).
- Strengthened the `test_event_follow` assertion: instead of masking the exit code with `|| true` and asserting success (always true), it now runs `timeout 1 mngr event my-task --follow` directly and asserts exit code 124, which proves the follow stream actually started and kept streaming for the full second rather than crashing on startup. It also asserts no traceback leaks to stderr.
