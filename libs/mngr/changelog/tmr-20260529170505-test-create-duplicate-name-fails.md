- Strengthened the `test_create_duplicate_name_fails` e2e release test: in addition to asserting the duplicate `mngr create` fails, it now verifies the failure is a name-collision error (the message mentions the agent name, "already exists", and the `--reuse` hint) and that the original agent is left untouched -- it is still running its original command (`sleep 100099`) while the rejected duplicate's command (`sleep 100123`) never started, confirmed via `mngr exec ... ps`. Added a `@pytest.mark.timeout(120)` to give the extra `exec` step headroom beyond the default 10s per-test budget.
