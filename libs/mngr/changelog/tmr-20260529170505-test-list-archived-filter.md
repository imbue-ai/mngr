- Fixed the `test_list_archived_filter` e2e tutorial test (`mngr list --archived`): removed its superfluous `@pytest.mark.modal`. The test only runs `mngr list`, which reaches Modal via in-process gRPC; that path is not visible to the resource guard across the `mngr` subprocess boundary (only the `modal` CLI binary is wrapped), so the mark could never be satisfied and the test failed the "never invoked modal" guard. Also strengthened the test to assert the archived filter actually produces an empty listing ("No agents found") in a fresh environment, not merely a clean exit.
