Fixed the `test_recipe_multi_agent_parallel_workflow` e2e tutorial test so it reliably passes. Calibrated its timeouts to the real cost of running the recipe (each `mngr` invocation pays a fixed ~15-20s CLI startup in the all-packages dev/test venv): raised the test-level timeout and gave the provisioning `create`, the fan-out pipes, and `destroy` enough per-command headroom. Also removed a superfluous `@pytest.mark.rsync` mark, since the local command-agent stand-ins never invoke rsync.
