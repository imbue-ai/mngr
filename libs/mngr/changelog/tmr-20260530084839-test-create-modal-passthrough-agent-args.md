- e2e test fixture: seed a default agent type (`[commands.create] type = "claude"`) in the test profile's local settings, mirroring what `install.sh` does for real users. The Modal `create` e2e tests (e.g. the agent-arg passthrough block) run `mngr create` without an explicit `--type`, which now requires a configured default since the source-coded `claude` default was removed. The type is written into the same settings layer as `connect_command` so both share `commands.create.defaults` and don't trip the settings-narrowing guard.
- Strengthened `test_create_modal_passthrough_agent_args` to also confirm via `mngr list` that the remote agent was actually created, rather than only asserting a clean exit code.
