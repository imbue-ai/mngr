Marked the three heaviest claude-on-Modal e2e create tests (`test_create_modal_no_connect_message`, `test_create_modal_edit_message`, `test_create_modal_passthrough_agent_args`) as `@pytest.mark.flaky` so offload retries them automatically. Each provisions a real Claude Code install on a remote Modal host -- slow and not validated locally -- which makes them prone to spurious timeouts/install hiccups.
