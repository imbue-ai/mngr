- Fixed the `test_destroy_short_form` tutorial e2e test (covering the `mngr rm my-task` short-form for `mngr destroy`). It previously fell back to the global 10s pytest timeout and was incorrectly marked `@pytest.mark.modal` even though it exercises a local command agent (so the modal resource guard flagged it once the body ran to completion). It now has an appropriate `@pytest.mark.timeout` and only declares the resource markers it actually uses (`rsync`, `tmux`).
- Strengthened the test to verify real behavior: it stops the running agent first (a bare `mngr rm`/`mngr destroy` without `--force` refuses to destroy a running agent) and then asserts that `mngr rm` reports `Destroyed agent: my-task` and that the agent no longer appears in `mngr list`.
