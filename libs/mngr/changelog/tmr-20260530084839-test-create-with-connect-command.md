- e2e test `test_create_with_connect_command`: removed the superfluous `@pytest.mark.modal` mark (the test creates a local `command` agent and only ever runs `mngr create`/`mngr list`, which reach Modal via the in-process Python SDK in the subprocess and are therefore not visible to the Modal CLI resource guard, so once the test body passed the guard correctly flagged the mark as `NEVER_INVOKED`) and added `@pytest.mark.timeout(120)` so local agent creation (git + rsync + tmux setup) has enough time to complete in the slow offload sandbox where the default 10s function timeout otherwise fires.
