- Fixed the `test_list_local_filter` e2e tutorial test, which was failing under the resource guard because it carried a superfluous `@pytest.mark.modal`. The `mngr list --local` command restricts discovery to the local provider and never queries Modal, so the mark is now removed. Also strengthened the test to assert the command reports "No agents found" in the isolated environment, rather than only checking the exit code.
