Fixed the `test_create_modal_basic_recap` e2e tutorial test, which broke after agent creation began rejecting a missing agent type instead of falling back to a default. The test now passes `--type claude` (the tutorial's canonical default agent) as an extra flag so the "basic Modal agent" command exercises the intended path. Also strengthened the test to verify the created agent is actually registered as a Modal agent rather than only checking the exit code.
