Fixed the `test_create_with_env` e2e tutorial test (env-var creation), which carried `@pytest.mark.rsync` but never invoked rsync. Worktree creation only rsyncs files when the source tree has uncommitted changes to carry over, so the test now dirties the working tree with an untracked file before `mngr create --no-ensure-clean`, matching the established pattern in `test_create_with_no_ensure_clean`.
