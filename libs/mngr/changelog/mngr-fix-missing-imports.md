Fixed missing imports and a missing fixture parameter in the e2e tutorial test suite (`json`, `re`, `Path`, `Any`, `sys`, and the `temp_git_repo` fixture) that were introduced by a bulk merge and caused F821 errors. Also fixed a stale `_create_my_task` call signature, removed an unused `json` import, deduplicated stray imports, and deleted a dead duplicate transcript-staging helper in `test_transcript.py`. No user-facing behavior change.
