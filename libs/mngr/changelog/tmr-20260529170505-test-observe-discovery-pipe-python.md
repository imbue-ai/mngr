- Fixed the `test_observe_discovery_pipe_python` e2e tutorial test (SCRIPTING AND AUTOMATION section) by removing its spurious `@pytest.mark.modal` mark. `mngr observe --discovery-only` performs read-only discovery that never invokes the Modal CLI (and its subprocess gRPC calls are not visible to the resource guard), so the mark could never be satisfied and the resource guard failed the otherwise-passing test with "marked with @pytest.mark.modal but never invoked modal".
