- Fixed the MULTI-AGENT WORKFLOWS recipe in `mega_tutorial.sh`: the "send a coordination message to all agents" step used `mngr msg -a`, but the `-a`/`--all` flag was removed from multi-target commands. It now uses the supported pattern `mngr list --ids | mngr msg - -m "..."`, matching the recipe's neighboring `mngr list --ids | mngr exec -` step.
- Corrected and strengthened the corresponding e2e test `test_recipe_multi_agent_parallel_workflow`: removed the inapplicable `@pytest.mark.modal` marker (the test substitutes local command-type sleep agents and never invokes Modal) and added behavioral assertions that the exec/message fan-out reaches all three agents, that `mngr destroy --remove-created-branch` reports destroying all three and deleting each branch, and that the agents and their branches are actually gone afterward.
