Test-only: strengthened the `mngr create --upload-file ... --extra-provision-command ...` Modal tutorial e2e test (`test_create_modal_upload_and_extra_provision_command`). It now verifies the tutorial's documented "provision commands run as the host's default user" behavior by recording the provisioning user and asserting it matches the user the agent's own commands run as on the host. It also switches the create to the lightweight `--type command -- sleep` agent (matching the rest of the Modal create tests), which exercises the same provisioning code path without the slower default-claude install and its spurious "no API credentials" warning.
