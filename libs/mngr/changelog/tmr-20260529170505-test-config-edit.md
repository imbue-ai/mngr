- Strengthened the `mngr config edit` release test (`libs/mngr/imbue/mngr/e2e/tutorial/test_config.py::test_config_edit`): instead of merely forcing `$EDITOR=/bin/true` and asserting exit 0, it now points `$EDITOR` at a script that appends a setting and verifies the round-trip -- the project config file is created on demand, the command reports the file it opened, and the edit persists to disk.
- Added `test_config_edit_editor_failure`, an unhappy-path test sharing the same tutorial block, which confirms that `mngr config edit` propagates a non-zero exit when the editor itself fails (`$EDITOR=false`).
