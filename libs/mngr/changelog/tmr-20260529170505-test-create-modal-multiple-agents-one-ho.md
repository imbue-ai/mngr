- Fixed the release e2e test `test_create_modal_multiple_agents_one_host` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_modal.py`), which failed with "No agent type provided" because the isolated test environment configures no default agent type. The two `mngr create` commands now pin `--type command -- sleep N` (the established fast-agent pattern used throughout the e2e suite); the tutorial block itself is unchanged.
- Strengthened the same test's assertions to verify the behavior the tutorial block actually teaches: both agents land on the same Modal host (compared via `mngr list --format json`), and stopping one agent leaves the other active with the shared host still running.
