Fixed the `test_destroy_by_session_name_happy_path` e2e tutorial test so it verifies exactly its documented scope: removed the incorrect `@pytest.mark.rsync` mark (the local command-agent flow never invokes rsync, which the resource guard confirmed), raised the per-test timeout from 60s to 120s to fit the create-plus-Modal-GC destroy runtime, and scoped the final verification listing to `mngr list --provider local` so it does not hang on the enabled-but-slow Modal backend (matching the convention used across the rest of the e2e suite).
