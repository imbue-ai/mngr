Fixed the `test_create_address_syntax_existing_host` release test so it actually exercises the existing-host lookup path. The test now passes `--type claude` (the isolated e2e profile has no default agent type, so the command previously failed at agent-type resolution before reaching host lookup) and dropped the superfluous `@pytest.mark.modal` mark (the command fails fast on the missing host and never invokes Modal). Assertions now verify the error names the specific missing host (`my-dev-box`).
