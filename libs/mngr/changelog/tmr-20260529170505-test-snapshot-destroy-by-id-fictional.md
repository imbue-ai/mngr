- Corrected the "destroy a specific snapshot" example in `mega_tutorial.sh`: `mngr snapshot destroy --snapshot snap-123abc` was missing the required agent argument (the CLI rejects it with "Must specify at least one agent"). It now reads `mngr snapshot destroy my-task --snapshot snap-123abc`, matching the command's own help examples.
- Fixed and strengthened the snapshot tutorial e2e tests (`libs/mngr/imbue/mngr/e2e/tutorial/test_snapshot.py`): the shared modal-agent helper now passes `--type command -- sleep ...` (it previously failed with "No agent type provided"), every test that creates a modal agent is now marked `@pytest.mark.rsync`, and `test_snapshot_destroy_by_id_fictional` now provisions a real host and asserts that destroying a non-existent snapshot id exits non-zero with a "snapshot not found" error.
