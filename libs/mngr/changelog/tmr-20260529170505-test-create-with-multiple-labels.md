- Fixed the `test_create_with_multiple_labels` e2e tutorial test (`libs/mngr/imbue/mngr/e2e/tutorial/test_labels.py`): removed a spurious `@pytest.mark.modal` mark that tripped the resource guard (the test creates a local command-type agent and never invokes modal), and strengthened it to verify that the `--label team=backend --label priority=high` values are actually attached to the created agent by inspecting `mngr list --format json`, rather than only checking that `mngr create` exits 0. Added a `@pytest.mark.timeout(180)` override because the verification `mngr list` call's provider-discovery setup exceeds the global 10s test timeout.
