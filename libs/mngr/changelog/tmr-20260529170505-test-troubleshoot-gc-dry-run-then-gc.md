- Fixed the release test `test_troubleshoot_gc_dry_run_then_gc` in `libs/mngr/imbue/mngr/e2e/tutorial/test_troubleshooting.py`. It was failing two ways: the default 10s pytest timeout fired before the real `mngr gc` finished, and the `@pytest.mark.modal` resource guard reported `NEVER_INVOKED` because `mngr gc` short-circuits Modal entirely when no Modal environment exists yet (nothing has been created there). The test now creates a live Modal command agent first so gc has real provider state to scan, raises the per-test timeout to 300s, adds `@pytest.mark.rsync` (the create transfers the repo), and asserts that gc preserves the still-running agent (`mngr list` still shows `my-task`). The tutorial block remains unchanged.
