- Fixed the e2e tutorial fixture (`libs/mngr/imbue/mngr/e2e/conftest.py`) to set `[commands.create] type = "claude"` in the test profile. The `mngr create` agent-type default was moved into user config (written by `scripts/install.sh`), so tutorial blocks that omit `--type` (e.g. `mngr create my-task --provider modal -b ...`) were failing in e2e with "No agent type provided". The fixture now mirrors a real installed environment, matching the tutorial's assumption that claude is the default agent type.
- Strengthened `test_create_modal_build_args` to verify the `-b image=python:3.12` build arg actually takes effect by running `mngr exec my-task "python3 --version"` on the provisioned host and asserting it reports Python 3.12, rather than only checking that the create command exits 0.
