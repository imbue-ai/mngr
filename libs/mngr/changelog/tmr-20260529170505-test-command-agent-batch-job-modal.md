- Strengthened the `test_command_agent_batch_job_modal` e2e test (RUNNING NON-AGENT PROCESSES tutorial section). Beyond asserting the `mngr create batch-job --provider modal --type command --idle-mode run --idle-timeout 30` command exits cleanly, it now inspects `mngr list --provider modal --format json` to confirm the agent was actually created as a command-type agent with the batch command preserved, `idle_mode` of "run", and a 30s idle timeout. The reconnect step (`mngr conn batch-job`) now uses a generous timeout (so it tolerates restarting a stopped host) and asserts the connection output references the agent. The tutorial block itself is unchanged.
