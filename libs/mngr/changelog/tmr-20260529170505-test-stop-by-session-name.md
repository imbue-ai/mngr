- Fixed the `test_stop_by_session_name` tutorial e2e test (in `libs/mngr/imbue/mngr/e2e/tutorial/test_lifecycle.py`). It previously carried `@pytest.mark.modal` but never invoked Modal (its command errored at argument parsing before reaching any provider), so the resource guard failed it. It now creates a real command-type agent and stops it by its actual tmux session name (`${MNGR_PREFIX}my-task`), then asserts via `mngr list --format json` that the agent is `STOPPED`. Marks were corrected to `@rsync`/`@tmux`/`@release` to match the resources a local create+stop actually uses.
- Added `test_stop_by_session_name_invalid_prefix` covering the unhappy path: `mngr stop --session my-session-name` is rejected with a "does not match the expected format" error before touching any provider.
