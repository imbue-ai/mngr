Raised the per-test timeout on the `test_unknown_command_fails` e2e tutorial test. The single `mngr` subprocess it runs has to cold-start the full CLI (plugin/provider registration), which on its own routinely approaches or exceeds the global 10s pytest timeout in the e2e environment, causing spurious timeout failures even though the command itself behaves correctly (exits with code 2 and points the user to `mngr --help`).
