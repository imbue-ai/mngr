- Fixed the release e2e test `test_create_modal_edit_message` (under `libs/mngr/imbue/mngr/e2e/tutorial/test_create_modal.py`). The command now passes an explicit `--type command -- sleep ...` agent type (the isolated test profile configures no default type) and points `$EDITOR`/`$VISUAL` at `true` so the `--edit-message` editor exits non-interactively. The test also now verifies the agent was genuinely created on a Modal host via `mngr list --provider modal --format json`, and its per-test timeout was raised to accommodate the extra remote call.
