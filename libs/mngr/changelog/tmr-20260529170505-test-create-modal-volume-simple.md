Fixed the `test_create_modal_volume_simple` e2e tutorial test, which failed because `mngr create` no longer ships a built-in default agent type: the command now passes an explicit `--type command` (matching the convention used across the e2e suite). Also strengthened the test to verify the Modal volume is actually mounted and writable at the requested path, instead of only checking that `mngr create` exited successfully.
