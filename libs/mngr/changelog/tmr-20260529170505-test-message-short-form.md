- Fixed the `test_message_short_form` tutorial e2e test (covering `mngr msg`): added a `@pytest.mark.timeout(120)` so the test function has room to both create an agent and send a message (the default 10s function timeout was too short), and removed a superfluous `@pytest.mark.modal` mark since the test only exercises a local command agent. Strengthened its assertions to verify the message was actually delivered, not just that the command exited 0.
