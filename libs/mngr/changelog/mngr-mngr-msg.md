Fixed `mngr transcript` (human format) rendering assistant messages as `(no content)` when the event carries its text in the flat `text`/`tool_calls` fields rather than an ordered `parts[]` list. Claude's `common_transcript` emitter fills only the flat fields, so every assistant turn -- including ones with real text -- previously collapsed to `(no content)`; the formatter now falls back to the flat fields when `parts[]` is absent.
