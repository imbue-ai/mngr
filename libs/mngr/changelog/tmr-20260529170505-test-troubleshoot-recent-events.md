- Fixed the `test_troubleshoot_recent_events` tutorial e2e test (for `mngr event my-task --tail 20`): it created a local command-type agent that never invokes Modal, so the `@pytest.mark.modal` resource-guard mark was superfluous and failed the test. Removed the `modal` mark (keeping `rsync` and `tmux`, which the local agent does exercise).
- Strengthened the test to validate that any emitted events are well-formed JSONL with the guaranteed envelope fields (`event_id`, `timestamp`, `source`, `type`), and added an unhappy-path test asserting that `mngr event` on a non-existent agent fails with a clear "Could not find agent" error.
