Fixed the `test_list_combine_exclude_filters` tutorial e2e test (LABELS AND FILTERING section). It now creates a labeled Modal `command` agent before running the `mngr list --exclude ... --exclude ...` command, so the test actually exercises the Modal CLI (satisfying the `@pytest.mark.modal` resource guard, which a read-only `mngr list` against a fresh environment never triggers) and verifies the OR semantics of multiple `--exclude` filters: an agent matching any exclusion clause is dropped, while one matching none is kept.
