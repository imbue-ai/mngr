Fixed the `test_snapshot_create_checkpoint` e2e tutorial test: the setup `mngr create --provider modal` step now passes an explicit `--type command -- sleep ...` since the isolated e2e profile configures no default agent type (the installer normally sets `commands.create.type`). Also strengthened the test to verify the created snapshot actually appears under its given name in `mngr snapshot list`.
