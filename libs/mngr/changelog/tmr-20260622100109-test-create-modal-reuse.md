Strengthened the `test_create_modal_reuse` e2e test for `mngr create --reuse --provider modal`. It now asserts on the observable signals of each branch of `--reuse`: the first (creating) run reports starting a fresh agent and does not report reuse, while the second run reports "Reusing existing agent". It also verifies the reused agent's host is left RUNNING, matching the tutorial's "(and started)" promise.
