Strengthened the `mngr create` template-stacking e2e test (`test_create_stack_templates`) so it confirms that *both* stacked templates are applied, not just that the command exits 0. The `with-tests` template now sets an observable environment variable that `mngr create` writes to the agent's env file, and the test verifies it via `mngr exec`, alongside the existing check that `modal-big` runs the agent in-place.
