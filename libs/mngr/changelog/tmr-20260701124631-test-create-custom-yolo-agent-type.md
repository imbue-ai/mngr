Fixed the `test_create_custom_yolo_agent_type` tutorial e2e test. It previously created the project config with `config edit` and then defined the custom `yolo` agent type via `mngr config set`, which failed because the freshly-created project config file did not opt into pytest runs (`is_allowed_in_pytest`). The test now drives `config edit --scope project` with a fake editor that writes the `[agent_types.yolo]` section directly into the project config file (mirroring the tutorial's manual edit) along with the pytest opt-in, then creates the agent with `mngr create my-task yolo`.
