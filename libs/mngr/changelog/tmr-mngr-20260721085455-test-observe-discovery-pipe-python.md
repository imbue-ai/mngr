- Fixed the `test_observe_discovery_pipe_python` e2e tutorial test so it verifies the current discovery stream behavior: `mngr observe --discovery-only` is a producer that emits per-provider `DISCOVERY_PROVIDER` snapshots from its own startup poll (the legacy whole-world `DISCOVERY_FULL` snapshot is deprecated and no longer produced), and its capture window now clears the mngr subprocess cold-start so the stream reliably carries a snapshot end to end into the piped python one-liner.
