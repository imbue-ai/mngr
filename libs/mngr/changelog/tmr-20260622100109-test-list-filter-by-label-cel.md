Test-only: fixed the LABELS tutorial e2e test `test_list_filter_by_label_cel` so it reliably passes. The CEL-filter verification list is now scoped to `--provider local` so it does not fan out to every configured backend; previously the full discovery aborted (the AWS backend has no credentials in the test environment and `mngr list --on-error` defaults to abort) or stalled past the per-command timeout waiting on an unreachable remote provider. The superfluous `@pytest.mark.rsync` mark was also removed, since this local-only test never invokes rsync.
