- Fixed the release-marked e2e test `test_create_custom_yolo_agent_type` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_agent_types.py`), which covers the custom-agent-type tutorial block. The test now defines the custom `yolo` type by writing a complete `[agent_types.yolo]` block (with both `parent_type = "command"` and a pinned `command`) into the project `settings.toml`, simulating the editor edit the tutorial describes. Previously it set only `agent_types.yolo.command`, which left the type with no resolvable agent class, and it wrote a project config that lacked the `is_allowed_in_pytest` opt-in required by the per-file pytest guard. The spurious `@pytest.mark.modal` mark was also removed: the test creates a local agent and never exercises Modal, so the resource guard's "marked but never invoked" check was failing.
- Strengthened the test's assertions: after creating the agent it now lists the local agents as JSON and verifies the created agent is recorded with `type == "yolo"` and the configured `command`, rather than only checking that `create` exited 0.
