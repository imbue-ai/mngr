- e2e test `test_create_with_json_output`: removed the superfluous `@pytest.mark.modal` mark (the test creates a local `command` agent and never invokes the Modal CLI, so the resource guard flagged the mark as unused) and added `@pytest.mark.timeout(120)` so local agent creation has enough time to complete in the slow offload environment. Also tightened the assertions to parse the `--format json` output from `mngr create` and verify it round-trips against `mngr list --format json`.
