- Fixed the release-marked e2e test `test_event_follow_filter_source` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_event.py`): removed the superfluous `@pytest.mark.modal` mark (the test only creates a local `command`-type agent and reads its events, so it never invokes modal, which tripped the resource-guard superfluous-mark check), and added `@pytest.mark.timeout(60)` because the real `mngr create` it performs sits right at the 10s global pytest timeout and intermittently exceeded it.
- Strengthened the same test: instead of wrapping the command in shell `timeout 1 ... || true` (which always exited 0 and masked any crash), it now runs the tutorial block's command verbatim (`mngr event my-task --follow claude/common_transcript`) under the harness timeout and asserts exit code 124, confirming the `--follow` stream stays open for the full window rather than crashing or rejecting the source filter.
