- Fixed: `test_create_short_forms` e2e test now passes locally. Added `@pytest.mark.timeout(300)` so the test isn't killed by the default 10-second pytest-timeout, and removed an incorrect `@pytest.mark.modal` mark (the test uses the local provider via `mngr create`/`mngr c`/`mngr list`, which never invokes the `modal` CLI binary that the resource guard tracks).
