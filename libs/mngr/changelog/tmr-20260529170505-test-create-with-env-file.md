- Fixed the tutorial e2e test `test_create_with_env_file` (under `libs/mngr/imbue/mngr/e2e/tutorial/test_env_vars.py`): removed a superfluous `@pytest.mark.modal` marker. The test creates a local `--type command` agent with `--no-connect` and never invokes Modal, so the resource guard was failing it on the "marked but never invoked" check. Also strengthened the test to verify the env-file variables are actually loaded into the agent's on-disk environment, not just that the create command succeeds.
