- Fixed the `test_advanced_observe_stream` e2e tutorial test: dropped the superfluous `@pytest.mark.modal` (the `mngr observe --discovery-only` discovery path never invokes the guarded `modal` CLI binary, so the resource guard flagged the mark as never-invoked), and strengthened it to give the stream time to emit and then assert that the JSONL output is well-formed and includes the authoritative `DISCOVERY_FULL` snapshot.
