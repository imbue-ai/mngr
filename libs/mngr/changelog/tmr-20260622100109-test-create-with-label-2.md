Fixed the `test_create_with_label` tutorial e2e test so it reliably passes regardless of ambient cloud credentials: it now scopes its `mngr list` calls to `--provider local` (matching the sibling local-agent tests) so discovery no longer hard-fails when an unconfigured cloud backend like AWS reports itself unavailable, and it drops the superfluous `@pytest.mark.rsync` mark (the agent uses the default git-worktree transfer and never invokes rsync).
