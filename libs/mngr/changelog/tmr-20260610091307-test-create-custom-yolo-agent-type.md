Fixed the `test_create_custom_yolo_agent_type` e2e tutorial test. It now follows the tutorial faithfully by driving `mngr config edit --scope project` with a fake editor that injects the `[agent_types.yolo]` block into the project config, rather than using `config set`. Previously the test created an empty project `settings.toml` (via a no-op editor) that lacked the `is_allowed_in_pytest` opt-in, so the subsequent `config set`/`create` commands were rejected by the pytest config guard.
