Scoped the `mngr list` verification in the `mngr create --format json` tutorial e2e test to the local provider (`--provider local`). An enumerate-all `mngr list` intentionally fails loudly when a registered backend is unavailable (e.g. the AWS plugin installed without credentials), so the test now targets the provider the agent actually lives on, matching the convention already used by other e2e tests.
