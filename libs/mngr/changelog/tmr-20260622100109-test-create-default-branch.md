Fixed the `test_create_default_branch` e2e release test (covering the default-branch-per-agent tutorial block). Removed a stale `@pytest.mark.rsync` mark: the default git-worktree transfer only rsyncs extra (uncommitted/untracked/gitignored) files, and the e2e fixture's repo is clean, so rsync is never invoked and the resource guard failed the otherwise-passing test. Also raised the `mngr create` step's command timeout to 60s so the one-time `ttyd` install attempt during agent creation does not exceed the 30s default under load.
