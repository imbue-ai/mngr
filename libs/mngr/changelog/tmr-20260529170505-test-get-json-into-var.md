Removed the superfluous `@pytest.mark.modal` mark from the `mngr list --format json` scripting e2e test: the command only reaches Modal via the in-process SDK, which the subprocess resource guard cannot observe, so the mark was unsatisfiable. Strengthened the test to parse the captured JSON and assert its documented `{agents, errors}` shape, and added a companion test that verifies a created agent actually appears in the JSON listing.
