Dropped the stale `@pytest.mark.rsync` mark from the `test_event_follow_filter_source` e2e tutorial test. The test runs entirely against a local command agent, which materializes its work dir via a git worktree (and only rsyncs uncommitted files, of which the clean e2e repo has none), so rsync was never invoked and the resource guard failed the otherwise-passing test. The `@pytest.mark.tmux` mark is retained.
