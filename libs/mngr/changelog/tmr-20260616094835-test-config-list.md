Strengthened the `mngr config list` e2e coverage: the default-view test now also asserts that a defaulted-but-unset field (`is_nested_tmux_allowed`) is hidden, pinning the "filtered to explicitly-set keys" contract. Added a new `test_config_list_all` covering the `--all` flag, verifying it surfaces every settable field (including the defaulted key the filtered view hides) while still reflecting persisted values.
