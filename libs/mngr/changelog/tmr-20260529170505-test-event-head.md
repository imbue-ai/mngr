- Fixed the `VIEWING EVENTS` e2e tutorial tests in `libs/mngr/imbue/mngr/e2e/tutorial/test_event.py`. They previously inherited the repo-wide 10s pytest timeout, which is too short for the per-test `mngr create` step, and carried a superfluous `@pytest.mark.modal` mark even though their bodies (a local `--type command` agent plus `mngr event`) never invoke Modal. Each test now uses `@pytest.mark.timeout(120)` and drops the unused `modal` mark.
- Strengthened `test_event_head` to verify the `--head 10` contract: output is parsed as JSONL, is capped at 10 lines, and every event object carries the documented guaranteed fields (`event_id`, `timestamp`, `source`, `type`).
