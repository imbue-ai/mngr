Fixed the `test_command_agent_dev_server_extra_windows` e2e release test so it passes reliably: bumped its per-test timeout to 60s (creating a command agent takes ~30s, exceeding the default 10s timeout) and removed the spurious `@pytest.mark.rsync` mark (the test creates a purely local git-worktree command agent that never invokes rsync, so the resource guard failed the otherwise-passing test).
