- e2e tests: the isolated test profile now sets a default `[commands.create] type = "claude"`, mirroring what `scripts/install.sh` writes for real users. This fixes the Modal create e2e tests (which run tutorial commands that omit `--type`) after the source-coded `claude` default was removed; without it every typeless `mngr create` failed with "No agent type provided". Tests that need a different type still pass `--type` explicitly to override.
- The `test_create_modal_dockerfile_and_context` release test now also asserts the custom `-b file=` Dockerfile was actually applied (the `python:3.12-slim` base triggers "not pre-installed in the base image" provisioning warnings), rather than only checking that the command exited 0.
