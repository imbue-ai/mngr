Fixed the `test_stop_basic` lifecycle e2e test: its post-stop verification now scopes `mngr list --stopped`/`--running` to `--provider local` (consistent with `test_stop_archive`), so it no longer fails when remote providers such as AWS are unavailable in the test environment. Also dropped the superfluous `@pytest.mark.rsync` mark, since stopping a local git-worktree command agent never invokes rsync.
