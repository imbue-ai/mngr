Fixed the `test_create_command_python_http` e2e tutorial test so its `mngr list` verification scopes discovery to the local provider (`--provider local`). The command agent is created on the local provider's default host, so fanning out to every configured provider was unnecessary and made the test fail in environments where a remote provider (e.g. AWS) is configured without credentials.
