Fixed the `test_command_agent_dev_server_extra_windows` e2e release test, which was timing out under the global 10s pytest-timeout. Added a `@pytest.mark.timeout(300)` marker (matching the sibling command-agent tests) and per-command timeouts on the `mngr create`/`mngr list` invocations so the test has enough headroom to cover the mngr CLI startup cost under contended release-suite sandboxes.
