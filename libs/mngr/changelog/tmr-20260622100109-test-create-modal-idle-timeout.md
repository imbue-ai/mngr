Test-only: added `test_create_modal_idle_timeout_invalid`, an unhappy-path companion to `test_create_modal_idle_timeout` covering the same tutorial block. It verifies that `mngr create --idle-timeout <not-a-duration>` is rejected with a clear "Invalid duration" error (naming the offending value) during option parsing, before any Modal host is provisioned. The test deliberately omits `@pytest.mark.modal` since the rejection happens before any Modal API call.
