- Corrected the SNAPSHOTS section of `mega_tutorial.sh`: the "list all snapshots" block previously showed `mngr snapshot list` with no arguments, but that errors out (`Must specify at least one agent or host`) since the `--all` flag was removed from multi-target snapshot commands. The block now lists snapshots across all agents' hosts via `mngr list --ids | mngr snapshot list -`, mirroring the adjacent `mngr snapshot create -` example.
- Updated the corresponding `test_snapshot_list` e2e release test to run the corrected pipeline, provision a modal command-type agent plus a snapshot, and assert the created snapshot actually appears in the listing.
