Fixed the `test_exec_git_push_then_merge` e2e tutorial test for the WORKING WITH GIT section. It now carries an explicit `@pytest.mark.timeout(120)` (the default 10s timeout was too short for agent create + exec + merge) and no longer carries the erroneous `@pytest.mark.modal` (the agent is created with the default local provider and never invokes Modal). The test also now asserts on the actual behavior: that `mngr exec` forwards the `git push` to the agent and surfaces the missing-`origin` failure, and that the caller-side `git fetch && git merge` integrates the agent's branch.
