Fixed the `test_create_headless` e2e tutorial test so it runs green: added a `@pytest.mark.timeout(120)` marker (the default 10s pytest timeout killed the `mngr create` subprocess mid-run) and removed the superfluous `@pytest.mark.modal` marker (a local headless command-agent create plus `mngr list` never invokes the `modal` CLI binary that the resource guard intercepts). Also strengthened the test to drive the headless agent non-interactively via `mngr exec` and assert on its output, verifying the agent is actually operational rather than merely present in `mngr list`.
