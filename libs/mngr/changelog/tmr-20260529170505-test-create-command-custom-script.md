- Fixed the release e2e test `test_create_command_custom_script` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_agent_types.py`). It was hitting the global 10s pytest timeout while a local `mngr create` agent was being provisioned, and it carried a spurious `@pytest.mark.modal` mark even though the command runs against the local provider (so the modal binary is never invoked, tripping the resource guard). Added `@pytest.mark.timeout(60)` plus an explicit per-command timeout, and removed the incorrect modal mark.
