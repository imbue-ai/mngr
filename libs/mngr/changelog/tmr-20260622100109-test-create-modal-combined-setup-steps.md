Strengthened the `test_create_modal_combined_setup_steps` e2e tutorial test so the second `--extra-provision-command` reads the uploaded `requirements.txt`, mirroring the tutorial's `pip install -r /workspace/requirements.txt`. This verifies the real ordering dependency the tutorial relies on: `--upload-file` must complete before the repeated `--extra-provision-command` steps run.
