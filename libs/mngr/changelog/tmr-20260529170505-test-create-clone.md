- Updated the BASIC CREATION block in `mega_tutorial.sh` that demonstrated creating a standalone clone: it now uses the supported `mngr create my-task --transfer=git-mirror` instead of the removed `--clone` flag (which no longer exists; transfer modes are now expressed via `--transfer`). The accompanying `test_create_clone` e2e test (`test_create_basic.py`) was updated to match and now strengthens its verification to confirm the clone lands in its own working directory (not the source, not a worktree), owns its own git directory, and carries over the source repo's history.
