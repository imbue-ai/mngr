Fixed the `test_exec_filtered_remote_disk` e2e tutorial test (LABELS AND FILTERING section). It now creates a Modal agent whose workspace lives at `/workspace` before running the filtered `mngr exec` so that the `host.provider == "modal"` filter has a real remote target, Modal is genuinely exercised (satisfying `@pytest.mark.modal`), and the test asserts that `df` actually ran on the remote agent.
