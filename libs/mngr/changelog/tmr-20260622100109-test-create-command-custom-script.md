Fixed the `test_create_command_custom_script` e2e tutorial test so its verification scopes `mngr list` to the local provider (`--provider local`), where the command agent is created. An unscoped `mngr list` fans out to every configured provider and exits non-zero if any one is unreachable (e.g. when AWS credentials are absent), which previously failed the test even though the local command agent was created and running correctly.
