- Fixed the release-marked e2e test `test_connect_explicit_host_and_provider` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_connect.py`). The `mngr conn my-task@my-host.modal` command fails at host-name lookup ("No hosts found") before ever invoking tmux, rsync, or the Modal API, so its `@pytest.mark.tmux`, `@pytest.mark.rsync`, and `@pytest.mark.modal` marks were superfluous and tripped the resource-guard "marked but never invoked" check. Removed those marks, leaving only `@pytest.mark.release`.
- Strengthened the test's assertions: in addition to a non-zero exit code, it now verifies the error output reports the missing host (`no hosts found` referencing `my-host.modal`), confirming the `agent@host.provider` syntax is parsed and reaches host discovery rather than crashing.
