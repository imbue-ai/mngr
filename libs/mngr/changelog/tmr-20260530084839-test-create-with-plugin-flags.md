- Added a happy-path e2e test (`test_create_with_disable_registered_plugin`) covering `mngr create --disable-plugin <registered>`: it verifies that disabling a real, registered plugin succeeds, does not trip the "not registered" guard, and still produces a working agent (confirmed via `mngr list` and by exec-ing a command inside the agent). This complements the existing unhappy-path `test_create_with_plugin_flags`, which checks the error for an unregistered plugin.
