- Fixed the `test_create_docker_cpus_start_arg` e2e tutorial test, which could never pass: it ran `mngr create --provider docker` with no agent type (failing with "No agent type provided") and used the start arg `-s cpus=2`, which `docker run` would have parsed as an image name. The test now passes `--type command -- sleep ...` (matching the other create tests) and uses the correct `--cpus=2` flag, plus a liveness check that the created container is execable.
- Corrected the matching `mega_tutorial.sh` block: `mngr create my-task --provider docker -s cpus=2` is now `-s --cpus=2`, since start args are forwarded verbatim to `docker run` and the resource-limit flag is `--cpus`.
