Fixed the `test_destroy_all_via_stdin` e2e release test: added a `@pytest.mark.timeout(300)` so it is not killed by the default 10s per-test timeout while creating two local agents, and removed the superfluous `@pytest.mark.modal` since the test uses the local provider and never invokes Modal (it does use rsync and tmux, which remain marked). Also strengthened the test to assert that the piped destroy reports both agents as destroyed.
