Fixed the `test_create_modal_no_start_on_boot` tutorial e2e test, which was failing because the isolated test environment configures no default agent type (so `mngr create --provider modal --no-start-on-boot` errored with "No agent type provided"). The test now passes an explicit `--type command` agent (with a long-sleep command), matching the fast-modal-test pattern used elsewhere in the file. Also strengthened the test to verify the flag actually took effect by asserting the created agent appears under `mngr list --include 'start_on_boot == false'` and is absent under `start_on_boot == true`, rather than only checking the create command's exit code.
