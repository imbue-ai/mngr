Fixed the `test_create_with_explicit_project` e2e tutorial test (PROJECTS section). It previously hit the global 10s pytest timeout because it lacked an explicit `@pytest.mark.timeout`, and it carried superfluous `@pytest.mark.modal` and `@pytest.mark.rsync` marks that the resource guard rejected (a local same-host create resolves to the `git-worktree` transfer mode and runs the command agent in tmux, so it invokes neither modal nor rsync). The test now sets an adequate timeout and declares only the resources it actually uses, and additionally asserts that the created agent is tagged with the explicitly requested project.
