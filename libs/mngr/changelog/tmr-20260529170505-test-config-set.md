Strengthened the `mngr config set` release test (`test_config_set`) to verify the concrete effect of the command: it now reads the project-scope `settings.toml` back from disk and asserts that `commands.create.provider` was actually written as `modal`, rather than only checking the exit code. Added a companion unhappy-path test (`test_config_set_rejects_unknown_field`) covering the same tutorial block, which asserts that setting an unknown top-level config field is rejected with an "Invalid configuration" error and nothing is persisted.
