- Fixed the `test_stop_archive` e2e release test in `libs/mngr/imbue/mngr/e2e/tutorial/test_lifecycle.py`: added a `@pytest.mark.timeout(300)` marker (the global 10s function timeout killed the test mid-run while creating and archiving the agent) and removed the spurious `@pytest.mark.modal` marker (the test creates a local command agent and never invokes Modal, which tripped the resource guard).
- Strengthened `test_stop_archive` to verify the actual effect of `mngr stop my-task --archive` rather than only its exit code: the agent now appears under `mngr list --archived` in the `STOPPED` state with an `archived_at` label, and is excluded from `mngr list --active`.
