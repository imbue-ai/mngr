Fixed the `test_create_codex_positional` tutorial e2e test so it verifies positional agent-type resolution without needing a codex binary on the host: it now disables the codex CLI install via `-S agent_types.codex.check_installation=false` (provisioning would otherwise try to `npm i -g @openai/codex` and fail where npm is absent). Also removed the incorrect `@pytest.mark.rsync` mark, since creating a codex agent from a local git repo uses a git worktree (not rsync), which tripped the rsync resource guard.
