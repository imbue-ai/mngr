Fixed the `test_config.py` e2e tests (`test_create_with_template` and `test_create_with_nonexistent_template`) timing out. They were the only e2e test module without an explicit `@pytest.mark.timeout`, so they fell back to the global 10s default, which is too short for a real `mngr` subprocess invocation (CLI startup alone takes ~14s). Added explicit timeout markers matching the other e2e tests.
