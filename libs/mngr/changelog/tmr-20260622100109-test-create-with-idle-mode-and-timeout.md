Scoped the verification `mngr list` in the `test_create_with_idle_mode_and_timeout` release test to `--provider modal`. The unscoped listing also probed every other registered backend (aws, azure, gcp, ...), which have no credentials in the test environment and so aborted the listing with a non-zero exit unrelated to the idle-mode behavior under test. This mirrors the provider-scoping already used by the analogous modal test in `test_command_agents.py`.
