Fixed the `test_message_filtered_via_stdin` tutorial e2e test, which falsely carried `@pytest.mark.tmux`, `@pytest.mark.rsync`, and `@pytest.mark.modal`. In the isolated test environment the `mngr list --include 'host.provider == "modal"' --ids | mngr msg -` pipeline is a pure local no-op (empty id list), so it never invokes those guarded resources. The superfluous marks now match the test's actual behavior. Also strengthened the test to verify the producer side of the pipe explicitly: the modal-filtered `--ids` listing is now asserted to succeed and emit an empty id list, in addition to the end-to-end pipeline succeeding.
