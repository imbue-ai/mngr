- `mngr list --fields "project"` (and `--format` templates referencing `project`) now resolve the short `project` field name to the underlying `labels.project` value, mirroring the existing `host.provider` -> `host.provider_name` alias and the `--project` filter flag. Previously `--fields "name,project,state"` rendered an empty PROJECT column even when agents had a project label.
- Hardened the `test_list_project_field` tutorial e2e test (`libs/mngr/imbue/mngr/e2e/tutorial/test_projects.py`): it now creates a Modal command agent tagged with an explicit project before listing, then asserts both the agent name and its project appear in the `mngr list --fields "name,project,state"` output. This exercises the project column end to end (the point of the tutorial block) and satisfies the modal resource guard.
