- Fixed: `test_create_and_rename_agent` in `libs/mngr/imbue/mngr/e2e/test_basic.py` no longer carries `@pytest.mark.modal` (the test creates a local agent and, after the May 2026 change that stopped `mngr list` from auto-creating the Modal environment, no `modal` CLI invocation reaches the resource guard). Adds `@pytest.mark.timeout(60)` so the test survives the 10s pytest-timeout default; the create+rename+list sequence routinely takes ~30s.
