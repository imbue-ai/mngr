Fixed the `test_config_set_default_provider` e2e tutorial test. It previously verified the persisted default provider with a follow-up `mngr config get --scope project`, but `mngr config set` writes a fresh project `settings.toml` that does not carry the `is_allowed_in_pytest = true` opt-in, so the follow-up command tripped the pytest config guard and failed. The test now reads the project `settings.toml` back with `cat` (matching the documented pattern in the e2e fixture) and asserts the value landed under `[commands.create]` as `provider = "modal"`.
