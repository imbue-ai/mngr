Fixed the `test_create_codex_explicit_type` e2e tutorial test so it runs green: it now configures the codex command in user scope (so the subsequent `mngr create` can load the config under pytest), allows enough time for a cold `mngr create` to finish, and only carries resource marks for the binaries it actually exercises. The test also now verifies the created agent really is of type `codex`.
