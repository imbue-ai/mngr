Fixed the `test_create_codex_positional` tutorial e2e test so it passes on hosts without `npm` (e.g. CI). The test now disables the codex CLI install check (`agent_types.codex.check_installation`) before creating the agent, so provisioning no longer attempts `npm i -g @openai/codex`; the test only exercises positional-argument agent-type resolution, not the codex binary. Also removed the now-stale `@pytest.mark.rsync` mark: with `--no-auto-start` in a git repo (git-worktree transfer), the create path never invokes rsync.
