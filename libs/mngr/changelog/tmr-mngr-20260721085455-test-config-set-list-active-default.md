Fixed the release test `test_config_set_list_active_default` (which covers the tutorial block for making a `mngr list` filter the config default). The test now runs reliably under the default per-test timeout, seeds the project config's pytest opt-in so its `mngr config get --scope project` read-back is accepted, and drops an inaccurate `@pytest.mark.modal` (the fresh-environment `mngr list` skips the Modal provider and never invokes the `modal` CLI). This is a test-only change with no user-visible behavior difference.
