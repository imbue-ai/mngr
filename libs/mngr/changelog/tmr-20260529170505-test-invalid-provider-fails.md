Strengthened the `test_invalid_provider_fails` e2e test so it genuinely exercises the invalid-provider failure path. Previously the test ran `mngr create` without an agent type, so the command failed on the "no agent type provided" gate before provider resolution was ever reached -- it would have passed even if the provider were valid. The test now supplies a valid `--type` so the command reaches provider resolution, asserts the error specifically names the unknown provider backend, and verifies that no agent was left registered.
