Fixed the `test_create_modal_no_connect_message` e2e tutorial test. It previously failed with "No agent type provided" because the isolated test profile has no default agent type. It now passes `--type command -- sleep` (matching the local `test_create_with_message` substitution), which still exercises the full message-on-launch path over the Modal provider (boot host, wait for readiness, deliver initial message), and it now asserts that the initial message was sent and that the agent landed on a Modal host.
