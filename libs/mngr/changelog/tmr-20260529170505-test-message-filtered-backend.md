- Fixed the `test_message_filtered_backend` tutorial e2e test (LABELS AND FILTERING section): removed the spurious `@pytest.mark.modal` mark (the filter+stdin message pipeline never invokes Modal in the isolated test environment, which tripped the resource guard) and added an explicit `@pytest.mark.timeout(60)` since multi-provider discovery runs right at the default 10s timeout.
- Added `test_message_filtered_backend_targets_only_backend`, covering the same tutorial block but creating real backend- and frontend-labeled agents so the test verifies the filter actually narrows the message to backend agents (and leaves the frontend agent untouched), rather than running as a no-op against an empty agent list.
