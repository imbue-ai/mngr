- Fixed the release-marked e2e test `test_create_modal_idle_timeout` (in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_modal.py`), which was failing with "No agent type provided" because the verbatim tutorial command omits `--type` and the e2e fixture configures no default agent type. The test now follows the convention used elsewhere in the file (e.g. `test_create_modal_idle_mode_run`): it runs `--type command -- sleep ...`, exercising `--idle-timeout 60` on Modal without a slow Claude startup.
- The same test now also verifies the idle timeout was actually applied (not just that the flag was accepted) by parsing `mngr list --provider modal --format json` and asserting `idle_timeout_seconds == 60` on the created agent.
