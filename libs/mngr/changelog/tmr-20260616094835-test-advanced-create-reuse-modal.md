Fixed the `test_advanced_create_reuse_modal` e2e release test so its verification `mngr list` calls scope discovery to the modal provider (`--provider modal`). The isolated e2e environment leaves every provider enabled, including unconfigured cloud providers such as aws, and `mngr list` exits non-zero when an enabled provider is unavailable; scoping keeps the assertion focused on the modal agent under test.
