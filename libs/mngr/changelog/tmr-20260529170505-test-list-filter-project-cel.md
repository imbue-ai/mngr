- Removed the erroneous `@pytest.mark.modal` from the e2e tutorial test `test_list_filter_project_cel` (`libs/mngr/imbue/mngr/e2e/tutorial/test_projects.py`). The test runs `mngr list --include 'project == "my-project"'` against a fresh environment with no agents; in that case `mngr list` intentionally skips the Modal provider rather than bootstrapping a Modal environment, so the test never exercises Modal and the mark caused a resource-guard "never invoked modal" failure. Also strengthened the assertion to confirm the CEL filter evaluates cleanly against an empty agent set.
