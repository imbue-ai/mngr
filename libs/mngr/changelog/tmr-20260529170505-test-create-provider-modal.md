Fixed the `test_create_provider_modal` e2e tutorial test, which failed because the isolated test profile has no default agent type configured (so the bare `mngr create my-task --provider modal` errored with "No agent type provided"). The test now makes the agent type explicit using the `command` type running a long `sleep`, matching the substitution used elsewhere in these tests, so it exercises the `--provider modal` path without a real claude install. Also strengthened the test to verify the agent actually lands on a Modal host via `mngr list`.
