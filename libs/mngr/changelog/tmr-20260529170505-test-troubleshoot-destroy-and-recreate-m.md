- Fixed the `test_troubleshoot_destroy_and_recreate_modal` e2e test: `mngr create` no longer has a hardcoded default agent type (the default moved into user config), so the test now passes an explicit `--type command -- sleep N` for both the initial create and the recreate, matching the convention used by the other tutorial e2e tests.
- Strengthened the same test to verify destroy/recreate behavior beyond exit codes: it now asserts via `mngr list` that the agent exists on a modal host before destroy, is gone after `destroy --force`, and is brought back on modal after recreate.
