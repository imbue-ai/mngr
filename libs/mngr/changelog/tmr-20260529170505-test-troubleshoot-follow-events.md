- Fixed the `test_troubleshoot_follow_events` tutorial e2e test: it was marked `@pytest.mark.modal` but only creates a local command agent and follows its events, so it never invokes the Modal CLI and the resource guard failed it. Removed the spurious `modal` mark and strengthened the assertion to confirm `mngr event --follow` was actively streaming (killed by `timeout`, exit 124) rather than merely exiting cleanly.
