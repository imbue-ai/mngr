Fixed the `test_list_with_no_agents` e2e release test to set an explicit `@pytest.mark.timeout(60)`. `mngr list` runs the full provider-discovery path (including an authenticated Modal environment lookup), which routinely takes ~10s and exceeded the default 10s per-test timeout when run locally. This matches the timeout override already used by the sibling filter tests in the same file.
