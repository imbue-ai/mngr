Fixed the `test_create_with_template` and `test_create_with_nonexistent_template` e2e tests so they no longer hit the global 10s pytest timeout: both now carry the `@pytest.mark.timeout(300)` override used by the other e2e tests. The `mngr exec` step in `test_create_with_template` is also scoped to the local provider (`my-task@localhost.local`) so agent discovery does not fan out to the remote Modal provider.
