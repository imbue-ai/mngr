Fixed the tutorial e2e tests for `mngr connect` (`libs/mngr/imbue/mngr/e2e/tutorial/test_connect.py`). The happy-path tests now exercise the real interactive `mngr connect` command inside a pty (so the blocking `tmux attach` does not hang or fail with "not a terminal") and assert that a tmux client actually attaches to the agent's session. Added per-test timeout overrides (the live agent operations exceed the 10s default) and corrected the resource-guard marks so each test declares only the resources it actually uses.
