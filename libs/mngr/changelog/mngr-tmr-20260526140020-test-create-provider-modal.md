- Fix the `test_create_provider_modal` e2e test: configure a default agent type at the start of the test (mirroring what `install.sh` does for real users) so `mngr create --provider modal` no longer fails with "No agent type provided", resync the in-test tutorial block to the current wording in `mega_tutorial.sh`, and tighten the assertion to verify the agent actually landed on the Modal provider rather than relying on exit code 0 alone.
