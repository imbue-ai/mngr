Fixed the `test_create_with_source_path_no_git` e2e tutorial test: removed the superfluous `@pytest.mark.modal` mark (the test creates a local `--type command` agent and never invokes Modal, which tripped the resource-guard `NEVER_INVOKED` check). Also strengthened the test to confirm the agent's work directory is a plain non-git folder, matching the tutorial block's claim that mngr does not require git.
