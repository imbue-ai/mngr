Removed the incorrect `@pytest.mark.rsync` mark from the `mngr event` tutorial e2e tests. These tests create a local command agent from the fixture's clean git repo, which uses the git-mirror transfer and never invokes rsync, so the resource guard was failing every test in the file with "marked with @pytest.mark.rsync but never invoked rsync". Updated the shared `_create_my_task` helper comment to explain why these tests carry the `tmux` mark but not `rsync` or `modal`.
