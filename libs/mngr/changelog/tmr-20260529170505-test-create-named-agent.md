- Fixed the release e2e test `test_create_named_agent` in `libs/mngr/imbue/mngr/e2e/tutorial/test_create_basic.py`: it now carries `@pytest.mark.timeout(120)` (the default 10s pytest timeout was too short for the create + list + `mngr exec` sequence) and no longer carries the superfluous `@pytest.mark.modal` mark (a local-provider test never invokes the `modal` CLI binary, so the resource guard correctly reported the mark as never-invoked).
- Strengthened the test to assert that `mngr exec my-task pwd` reports the agent's worktree `work_dir`, verifying the agent actually runs in its own worktree rather than only checking the command's exit code.
- Added a sibling test `test_create_generates_random_name_when_unnamed` covering the same tutorial block's documented behavior that omitting the name yields an auto-generated (multi-word, distinct) agent name.
