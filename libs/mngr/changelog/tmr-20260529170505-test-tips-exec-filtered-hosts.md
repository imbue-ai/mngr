Fixed the `test_tips_exec_filtered_hosts` e2e tutorial test so it actually exercises the documented workflow. It now creates a Modal agent before running `mngr list --include 'host.provider == "modal"' --ids | mngr exec - ...`, so the filter matches a real host and the exec runs on it (previously the pipeline no-op'd on an empty host list and the test could not satisfy `@pytest.mark.modal`). The test now asserts that the exec output contains the agent's `MNGR_AGENT_ID` and a per-agent success footer, and uses a longer timeout to accommodate Modal host provisioning.
