Fixed the `test_plugin_disable_affects_create` e2e release test so it verifies the intended behavior: while a plugin is disabled, `mngr create` fails specifically because the plugin is disabled (naming the remediation command), not because of the pytest config opt-in guard tripping on the freshly written project `settings.toml`. The test now seeds the project `settings.toml` with the pytest opt-in before disabling, and its timeout was raised from 60s to 300s (matching the sibling roundtrip test) to accommodate the several sequential real `mngr` invocations.
