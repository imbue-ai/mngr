- Fixed the tutorial-tied e2e test `test_create_git_mirror_with_existing_branch` (under `libs/mngr/imbue/mngr/e2e/tutorial/`): removed the superfluous `@pytest.mark.modal` mark, since the test creates a purely local agent (`--transfer=git-mirror --branch <current>`) and never invokes Modal -- the resource guard correctly flagged the unused mark. Also strengthened the assertions to confirm the agent is checked out on the existing branch itself (exact match, not a new `mngr/*` branch) and that the git mirror shares the source commit.
