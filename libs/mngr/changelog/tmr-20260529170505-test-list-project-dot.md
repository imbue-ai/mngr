Fixed the `mngr list --project .` tutorial e2e test (`test_list_project_dot`): removed an incorrect `@pytest.mark.modal` marker. In a fresh environment `mngr list` skips the Modal backend (it reports itself empty when no per-user Modal environment exists), so the test never exercised Modal and the resource guard rejected the superfluous mark. Also strengthened the test to assert the empty-list output and added `test_list_project_dot_filters_to_current_project`, which creates a local command agent and verifies that `.` resolves to the current project (the agent is listed) while a different project excludes it.
