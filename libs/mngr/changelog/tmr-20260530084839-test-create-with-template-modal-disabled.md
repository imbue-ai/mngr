Strengthened the e2e test `test_create_with_template_modal_disabled`: it now also runs the same create command without the template as a control, verifying that create succeeds with the default `local` provider. This proves the modal-provider failure is caused specifically by the template applying `provider="modal"`, and tightens the error assertion to require the word "modal" rather than the looser "modal|provider".
