When a VPS-backed host cannot be reached during discovery, the warning now names the provider (not just the IP) and states explicitly that any agents on that host are missing from listings until it is reachable again. Because this warning is logged by default whenever you run a command like `mngr connect`, a transiently-unreachable host is no longer mistaken for a nonexistent agent.
