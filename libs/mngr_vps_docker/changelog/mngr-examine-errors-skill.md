Adapted to the mngr SSH-key type change: the SSH key path is now `Path | None`. The human-readable SSH command shown for a discovered host is now built via `build_user_ssh_command`, which omits `-i` when there is no key path instead of emitting `ssh -i None`. No user-visible behavior change in the normal (keyed) case.
