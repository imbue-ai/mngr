Test-suite quality improvements (no production code or user-facing change): the six `emit_*` CLI tests now assert on captured stdout instead of only checking that the call did not raise; the outputs-archive pulling tests gained success-path coverage (publish a real archive to a local agent volume, then assert readiness flips and the members extract intact); the `_run_mngr_raw` subprocess test moved from the unit file to a `test_*.py` integration file with stronger assertions; and the `sanitize_for_agent_name` tests now pin the exact resulting slug.
