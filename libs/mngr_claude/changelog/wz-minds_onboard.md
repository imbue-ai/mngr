- mngr_claude: cap `security find-generic-password` reads at 10s so a hidden macOS keychain ACL prompt can no longer hang `mngr create` indefinitely. Symptom was Lima/remote agent creation stalled at "_setup_per_agent_config_dir" with no further log output (the `security` child was blocked forever waiting on a dialog the user never sees / dismisses). On timeout it returns None and agent creation continues without merging the keychain primaryApiKey.
