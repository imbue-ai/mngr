Removed `waiting_reason` from the README's "Not yet implemented" list: it is not applicable to opencode rather than a pending gap. opencode's permissions are an upfront config policy (`allow`/`deny`/`ask`) rather than an interactive prompt, so there is no permission-dialog to surface a reason for; the rationale is documented in the agent-plugin-parity spec.
