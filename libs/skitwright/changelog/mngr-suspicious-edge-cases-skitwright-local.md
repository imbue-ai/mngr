Hardened suspicious edge-case handling: the transcript formatter now branches on `OutputSource` with an exhaustive `match`/`assert_never` (so a future enum value becomes a type error instead of being silently formatted as stderr), and added clarifying comments to the `runner._reconstruct` empty-output guard and the `expect()` unsupported-type fallback explaining why each branch exists.
