Began generalizing `mngr usage` beyond Claude (groundwork; no user-visible change yet). Added a `TokenSnapshot` data type for usage sources that report token counts rather than a pre-computed dollar cost, and a `pricing` module (`compute_cost`) that turns tokens into USD via a curated per-token price table. The Anthropic prices mirror `apps/modal_litellm` (and were independently confirmed against a live pi session); an unknown model resolves to no estimate rather than a misleading `$0`.
