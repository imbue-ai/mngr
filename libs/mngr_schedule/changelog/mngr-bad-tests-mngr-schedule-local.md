Test-quality cleanup with no user-visible behavior change. Strengthened or removed low-value tests across the `mngr schedule` test suite: replaced tautological and "no exception raised" assertions with real behavior/effect checks, dropped tests that only asserted `NotImplementedError`, removed a monkeypatch-recorder test in favor of the existing real-behavior test, and pinned full-output expectations via `inline_snapshot` for the wrapper-script and `auto_fix_create_args` cases. Migrated hand-rolled git-repo setup and a fabricated `SimpleNamespace` `MngrContext` onto shared, system-config-isolated fixtures, moved the `isolated_home` fixture into conftest, and added `make_test_trigger` overrides plus `make_schedule_record`/`temp_git_repo_with_remote` fixtures. The Modal release tests now assert distinct, mode-specific signals for `--verify none/quick/full`. Lowered the `monkeypatch.setattr`, test-container-class, and args-in-docstring ratchet counts to 0.
