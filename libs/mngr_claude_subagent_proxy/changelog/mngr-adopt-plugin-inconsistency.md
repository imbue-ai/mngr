Added a guard test asserting `CLAUDE_SUBAGENT_PROXY_PLUGIN_NAME == "claude_subagent_proxy"`. mngr's core test infrastructure now disables this plugin during tests by that literal string (rather than importing this package, which would invert the layering); the guard test fails loudly if the config-registry key is ever renamed without updating those literals in lockstep. No runtime behavior change in this package.
