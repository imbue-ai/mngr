Added a conformance test asserting that antigravity's real emitted common-transcript records
validate against the new canonical envelope schema
(`imbue.mngr.agents.common_transcript_records`), so the antigravity emitter and the shared
contract cannot drift apart.
