- Added `codex` chat support: the live transcript, working/waiting status, and composer now render for `codex` agents too. As with `pi-coding`, foreman consumes codex's already-normalized *common transcript* (`events/codex/common_transcript/events.jsonl`, produced by `mngr_codex`'s `common_transcript.sh` from the raw rollout stream) through a thin normalizer (`codex_transcript.py`: validate + dedup on `event_id`, rename codex's `finish_reason` to the `stop_reason` the claude events use, re-cap tool output only when foreman is configured stricter than codex's own 2000-char cap) rather than a second bespoke parser. Codex is one more entry in the per-agent-type `TranscriptStrategy` registry (`harness.py`). It skips the `tmux capture-pane` `❯`-dialog detection: codex has no numbered-choice TUI dialogs, and when it blocks on a tool-approval prompt its mngr lifecycle state is promoted RUNNING→WAITING (via the `permissions_waiting` marker), so the working/waiting dot rides the same generic `active` RUNNING/WAITING signal claude and pi use — no needs-input pane scrape required. Known gaps for codex (accepted, same as pi): tool calls render from a ~200-char `input_preview` only (no `input_full`, so no diff view), and `model`/`usage` are absent (codex's converter does not populate them).
