Added authoritative tests for the permissions extension's `POST /permissions/rules` merge behavior, exercised end to end against the real `permissions.mjs` Node extension: re-granting the same scope replaces the rule in place (rather than appending a duplicate), and sibling top-level keys such as `schemas` survive a grant verbatim. This behavior was previously only exercised indirectly via a Python fake in the desktop client's tests.
