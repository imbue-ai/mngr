Added a new target-scoped `minds-workspaces-resize` verb to the workspace permission catalog, gating the agent-facing `POST /api/v1/workspaces/<id>/resize` endpoint. The verb is set-only: it grants changing a workspace host's CPU/memory allotment but no restart power (applying values that need a restart goes through the restart endpoint behind the existing `minds-workspaces-recover` grant).
