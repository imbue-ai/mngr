Creating a Modal host with `-b file=<path>` pointing at a nonexistent Dockerfile (or build-context directory) now raises a clear `MngrError` up front instead of surfacing a raw `FileNotFoundError` traceback.
